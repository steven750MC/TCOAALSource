// needs cleanup but i'm tired
const _0x403c = ["_globalId", "Award achievement (", "offc", "localFilePath", "Skipping auto-save. Remaining: ", "DevData/", "Format 1.0", "add", "pressed", "guard", "CSV missing columns.", "Invalid language file signature: ", "firstSavefileIndex", "basename", "allItems", "call", "editorX", "_listWindow", "_errorUrl", "property", "left", "saveGlobalInfo", "setupChoices", "boundaryWrap", "distance", "number", "#363636", "response", "clone", "pow", "operator for enable / ", "initialize", "_gamepadStates", "metaVarsRestore", "Invalid CSV parsing state.", "_pageIndex", "Vision Room", "seg_s", ".otf", "inputHint", "isDebugBuild", "commandRemember", "img/pictures/efbd6b41ff1cd6ba", "metaVars", "setLoadingImage", "data/9c7050ae76645487", "direction", "_loader", " to ", "LOOPLENGTH", "loadSystemWindowImage", "preventDefault", "apply", "img/parallaxes/c47a9c4ef24acf60", "arraybuffer", "global", "No valid escape direction.", "face", " in Map: ", "terms", "commandControls", "chainData", "finalPos", "Steam restarting...", "drawItem", "overrideMimeType", "isEnabled", "findFiles", "img/pictures/ef5d18e2e8d2e126", "langInfo", "decompressFromBase64", "pluginCommand", "linesLUT", "Game info file failed to load.", "_loadListener", "redirs", "MENUS", ".woff2", "contains", "ID (", "numItems", "setupNewGame", "img/parallaxes/7cb0d46c5adc0a03", "list", "_moveSpeed", "zOverride", "_priorityType", "file%1.rpgsave", "img/pictures/502fd28fe6e562a8", "_needsMapReload", "makeDeepCopy", "Error moving: ", "_success", "commandName", "\nFalling back to English.", "session", " -> ", "updateMessage", "data/Map%1.json", "Can't create chain when one already exists.", "createLoader", "Font failed to load: ", ".csv", "img/parallaxes/2b700449143c77d5", "Language name isn't set. Using default.", "isNormalPriority", "djb2", "img/parallaxes/8029b50924d441ef", "setChoiceBackground", "sections", "ERROR:  STEAM API FAILURE\n\n", "stringify", "APPDATA", "size", "controls", "_autoSave", "Controls", "isBusy", "isNwjs", "dekit", "wrap", "** ", "networkInterfaces", "LANGUAGE", "Game files corrupted.\nRe-installation may repair files.", "alwaysDash", "itemRectForText", "time", "commands", "decodeStream", "setBackground", "playSe", "balloon", "_updateGamepadState", "fontBold", "state", "isOptionValid", "pause", "pages", "rootPath", "eventsXy", "_trigger", "addLoadListener", "isValid", "resetAnimation", "img", "parameters", "_buffer", "hashBytes", "Volume BGM", "cursor", "VERSION", "Module failed to initialize Steam.", "LANGDATA", "isSteamRunning", "snapshotsRecord", "command101", "seg_o", "Cannot initiate Steam without NWJS.", " loaded in: ", "origin", "getConfigValue", "sysLabel", "byteLength", "boxHeight", "Failed to find language for font: ", "function", "initCommandPosition", "Steam language not retrieved.", "Failed to process buffered font: ", "anchor", "currentTilemap", "blt", "Can't set event Z index. No event ID: ", "log_", "Off", "setNewGameMap", "clearAchievement", "MBG", "_index", "isInitialized", "Corrupt Data", "Can't find default language.\nRe-installing game may fix it.", "loadDatabase", "fontSize < 1.", "addChild", "bgmVolume", "swap", "tag", "isTriggered", "_skips", "\nCheck with author.", " but got ", "usesEncryption", "Credits", "SPEAKERS", "keys", "User data error: ", "text_lut", "_waitCount", "Steam API failed to init from steam_appid.txt", "audioFileExt", "autoSaveSkip", "buttons", "_balloonId", "match", "loadNormalBitmap", "dialogue.csv", "appID", "Mstyle", "exit", "setHandler", "_readMetaData", "splice", "_databaseFiles", "_showFast", "Invalid global command.", "extractSaveContents", "itemRect", "_commandWindow", "Recovered Game", "name", "Steam achievements not cleared.", "moveFile", "renameSync", "createListWindow", "Steam action succeeded: ", "redirect", "chain3", "map", "loadPicture", "init", "Line is missing parts.", "createBuffer", "holdings", "createCharacters", "max", "translate", "file", "ERROR: Language file missing signature.", "path", "screenZ", "createGameObjects", "shift", "langName", "sqrt", "isNextScene", "rotation", "sin", "_params", "measureTextWidth", "redrawItem", "utf8", "img/parallaxes/5988afb69af8153b", "img/pictures/", "clearAllAchievements", "bitmap", "_loopStart", "img/parallaxes/f5307397750f3fe3", "_onLoad", "screenX", "seg_r", "tileWidth", "lines", "Missing column data for Section.", "getAchievementNames", "toUpperCase", "symbol", "scale", "Auto save failed.", "_reuseImages", "style", "disable comment hint: ", "playEquip", "addVolumeOptions", "user", "img/pictures/3abb168cc85141c4", "folders", "loadDataFile", "SECTION", "Window_Credits", "fontSize", "img/pictures/a48eba44af873aff", "create", "orderAndIcons", "Auto ", "isBattleTest", ".eot", "Default language missing or broken: ", "img/pictures/3c5c648ad067292c", "select", "isThisGameFile", "hasOwnProperty", "img/pictures/b8aa41515332fe56", "Clearing all achievements!", "title", "img/pictures/2e165e53783c6e41", "exists", "files", "pop", "cacheMem", "_failure", "screenName", "langVers", "delta", "clearCommandList", "_imageCache", "nw.gui", "Error deleting the file:", "Steam Module failed to init from steam_appid.txt.", "Steam API initialized.", "Couldn't locate language: ", "commandLanguage", "savesDisabled", "Unable to init data:", "savefileId", "Window", "buffer", ".cld", "areaData", "img/pictures/1f9dadbb8925302b", "newGameY", "mkdirSync", "log", "showDevTools", "localFileDirectoryPath", "cld:", "toFixed", "_url", "data/", "children", "_helpWindow", "invCurrent", "event", " parsed in: ", "sysMenus", "endsWith", "isOfficial", "env", "status", "startX", "changeValue", "hrtime", "_realX", "updateShowFast", "Steam module failed to load. Try:", "_background", "resize", "ERROR: ", "requiresVendor", "format", "loadCSV", "Steam achieved was stored: ", "toString", "_deltaTime", "guardValue", "credits", "fillRect", "Error reading file: ", "stopBgm", "new_game", "DxhMltBG", "data", "playtime", "createHash", "drawText", "bgsVolume", "Logs", "autoSaves", "Volume SFX", "statusText", "loadBitmap", "solve", "Item", "catch", "join", "none", "substring", "center", "loadGlobalInfo", "retryInit", "File search failed: ", "clearTransferInfo", "requestBitmap", "chain", "extname", "_optionsWindow", "reserveCharacter", "Steam failed to initialize for internal reasons.", "_mapLoader", "disable", "_newX", "newLang", "auto", ".log", "BAD_", "makeSavefileInfo", "invStorage", "Invalid hash: ", "_onError", "goto", "mac", "latestSavefileId", "LABELS", "ctrlKey", "_secondsPlayed", "seg_x", ".rpgsave", "lerp", "decode", "includes", "locate", "_mode", "_list", "height", "[CHOICES]", "startPos", "openness", "inv", "createContents", "steamAchvs", "values", "commandVisionRoom", "then", "_spriteset", "entries", "setupNumInput", "_interpreter", "Dotum", "mapId", "cursorRight", "LOOPSTART", ") Load failed: ", " empty.", "ext", "seg_y", "activateListWindow", "tags", "img/pictures/4299baade3e059ed", "get", "users", "setup", "_openness", "random", "Achievement: ", "cursorLeft", "loadMessage", "options", "_newY", "accessSync", "Wrong Version", "Steam initialized using steam_appid.txt.", "cacheSprites", "message", "isDebugCalled", "img/parallaxes/b3012b40acba6cdb", "initIter", "seg_w", "autoSaveCount", "moveStraight", "Game", "currentCommand", "processTextWrap", "performTransfer", "icon", "setText", "labelLUT", " **", "Info index out of bounds: ", "_textState", "src", "loadEmptyBitmap", "_skipCount", "#FFFF55", "heading", "Clear achievement: ", "steamId", "#30B0CF", "onCanvas", "CLD image buffer missing: ", "awardAchievement", "Audio stream failure: ", "\nFile: ", "target", "url(", "img/titles1/91b682859f543183", "byteOffset", "mainModule", "processOk", "_tilemap", "English", "Failed to load CLD image: ", "Fan translation missing or broken: ", "Invalid Data", "onload", "sort", "trim", "default", "reserveTransfer", "_sampleRate", "_scene", "attachTo", "updateState", "_active", "compressToUint8Array", "stamp", "readFile", "dialogue.txt", "_change", "img/pictures/057974b069d30654", "forEach", "readdirSync", "isBuffer", "_lngSelWin", "updateFrame", "createCommandWindow", "ERROR: Language data format invalid.", "isAttachedTo", "GET", "_fullDuration", "messages", "snapshotLoad", "restartAppIfNecessary", "smooth", "setupEventTest", "Missing column data for Item.", "\n - Verifying game file integrity.", "followers", "File Missing", "fontFile", "crash", "CoffinAndyLeyley/", "setTransparent", "playSave", "_switchFullScreen", "_img", "hideBackgroundDimmer", "Fast", "img/pictures/65c88a4e6682b6cc", "writeFile", "addEventListener", "cacheMB", "currentLanguage", "canAccess", "setChoiceCallback", "dataPath", "_input", "makeCommandList", "File is not in a valid format: ", "_requestFullScreen", "hash", "controlBitmap", "close", "delete", "Window_Message_setBackgroundType", "mkdir", "metaVarsRecord", "parent", "ERROR:  STEAM MODULE FAILURE\n\n", "sans-serif", "CoYQ8gghAyQAAA==", "positionType", "WARNING: ", "isAnySavefileExists", "dirname", "sha256", "Error copying: ", "Inventory argument missing.", "hasItem", "recoveryMeta", "Language property missing: ", "value", "extraLines", "_mapId", "\nTry reinstalling.", "7b11886681707ddf", "updateScene", "startY", "duration", "isWeapon", "drag", "onSaveSuccess", "loadSavefileImages", "Issue with file info: ", "open", "slice", "change", "#AAAAAA", "setBackgroundType", "code", "No autosave for intro map: ", "boxWidth", "cancel", "setupBattleTest", ".txt", "startsWith", "cos", "snapshotSave", "isReleaseBuild", "_dirItems", "background", "ashley", "saveData", "fullscreen", "SimHei", "meVolume", "Translation", "isFinite", "hostname", "Run Speed", "now", "saveMessage", "description", "test", "username", "prototype", "run", "img/parallaxes/ddcc76bcc94cafff", "terminate", "Unable to locate file info: ", "Error reading file from path: ", "onerror", "_loadingState", "execPath", "segImage", "Steam API module failed to import.", "dragover", "_windowLayer", "_onXhrLoad", "#FF5555", "img/pictures/6f901296c784f59e", "writeFileSync", "loseItem", "isSceneChanging", "Slow", "snapshotsRestore", "_requestImage", "toLowerCase", "process", "error", "imgFiles", "isCurrentSceneStarted", "playCancel", "append", "Game folder cannot be accessed: ", "speed", "maxSavefiles", "labels", "contents", "endpos", "reserve", "_lngMsgWin", "cacheSum", ") -> Version ", "chain2", "floor", "loadAllSavefileImages", "Missing langName.", "changeTextColor", "_onDecode", "alpha", "_errorListener", "substr", "mask", "save", "_gdat", "Fixed content ID: ", "internal", "drawIcon", "ERROR: Language file format invalid.", "w7ZvbBvC", "lastEvent", "WARNING:", "_newMapId", "Missing game folder: ", "preloadImportantSounds", "addCommand", "img/parallaxes/f7aa74a99761a884", "addWindow", "_generateCacheKey", "opacity", "$dataMap", "_duration", "bind", "#B2E087", "_iconWidth", "Unable to decompress language file: ", "T1lJS1heT0cEXlJe", ".svg", "metaVarsClear", "min", "send", "refresh", "findIndex", "isEditorBuild", "refreshActiveWindows", "_loadingImage", "globalGet", "isTriggerIn", "CSV first column missing.", "Language", "Global information lost.", "hasArg", "img/pictures/47b90b8577d12e80", "textSpeed", "read", "family", "filter", "img/pictures/163c685fda78a235", "achv", "onLoad", "maxopac", "text", "setMode", "languages/", "appendFileSync", "_lastAccessedId", "global.rpgsave", "Read Failed", "Can't set event Z index. No game map loaded.", "isClosing", "faces", "screenY", "loadCLD", "setConfigValue", "padding", "debug", "Missing fontFace.", "ITEMS", "_realY", "split", "cacheGet", "setChoicePositionType", "fillAll", "constructor", "reload", "frameIndex", "versions", "exePath", "update", "DESCRIPTIONS", "setupItemChoice", "commandQuitGame", "timestamp", "_isFullScreen", "relative", "speakers", ".png", "playtimeText", "storage", "_newDirection", "info", "start", "Game has persisting issues initializing Steam.\n\n", "loadGame", "You have some unawarded achievements, these will\n", "img/pictures/3eb1e91237c141ef", "object", "addGeneralOptions", "numVisibleRows", "setWaitMode", "vision_room", "width", "hashData", "base64", "setTopRow", "label", "Language file redirect: ", "Load", "checkPlayerLocation", "isCommandEnabled", "charCodeAt", "_context", "report", "notify", "No save file found with id: ", "waitTime", "Font Size", "CRITICAL ERROR: ", "language", "#EBAE69", "application/json", "4yhN4h9P", "Steam achievement already stored: ", "assign", "UI Hints", "window", "key", "hashPath", "_fpsMeter", "#182232", "filename", "No language table created.", "Run Always", "hasUserData", "img/pictures/84fdd590cc3c85b3", "GameFont", "editorY", "count", "argv", "quit", "Failed to get Steam ID.", "cacheAdd", "cachedSg", "Heiti TC", "set", "vision", "alert", "mapImages", "normal", "isTransferring", "hashString", "\nLine: ", "newData", "altKey", "warn", "abs", "findSymbol", "config.settings", "Successfully loaded font from CLD: ", "fail", "fps", "parse", "_loop", "nw-flavor", "Auto Saves", "changePaintOpacity", "index", "walkSpeed", "_renewCanvas", "copyFile", "is_header", "img/pictures/73912dfe6983d02b", "drawItemName", "fontFace", "newGameM", "new_block", "Error writing file to path: ", "reserveNormalBitmap", "removeChild", "img/system/e5230bf37c4fabb0", "requiresSteam", "windowWidth", "indexOf", "render", "DCRBH5zy", "startMapId", "Error writing to log file.", "snapshots", "Found custom translation at: ", "getSaveInfo", "iconImages", "Cannot delete missing file: ", "rgba(0, 0, 0)", "_balloon", "existsSync", "Can't search missing folder: ", "popScene", "clear", "isMapPassable", "updatePlayTime", "dashBonus", "loadUserData", " Found: ", "onSavefileOk", "setItem", "padZero", "segOffst", "Translation content is missing for: ", "seconds", "_loopLength", "isDirectory", "_eventId", "load", "textPadding", "from", "responseType", "segWidth", "compressToBase64", "xfer", "sortDesc", "API", "characters", "Save", "imsg", "No folder specified at: ", "fonts", "saveUserData", "CREDITS", "replace", "push", "updateDocumentTitle", "Missing lines in langInfo.", "setPositionType", "Can't save user data: ", "unpause", "commandNewGame", "saveGame", "_cancelFullScreen", "DEBUG: ", "img/pictures/5972d18a4e7f34a8", "autoSaveMax", "seVolume", "resetFontSettings", "current", "Instant", "fadeOutAll", "_decodeAfterRequest", "logPath", "createObjectURL", "Buffered font failed to load: ", "gainItem", "Fullscreen", "logFile", "onBeforeSave", "img/pictures/99293c069863c881", "editorM", "steamUsers", "Language data missing.\nRe-installing game may fix it.", "App", "Actor1", "hex", "Steam action failed: ", "makeSaveContents", "_image", "loadSystem", "globalDel", "Potential cache key collision: ", "decodeAudioData", "img/pictures/479165df84d30b92", "AppleGothic", ".woff", "img/parallaxes/0225d01841bb7841", "resetTextColor", "makeData", "length", "www/", "paused", "Unable to match image: ", "nextEventCode", "touchUI", "setChoices", "right", "img/pictures/8d0f07578e5fbaea", "body", "equals", "activeSg"];
(function (pushObj, rotateCount) {
  const shuffleFn = function (n) {
    while (--n) {
      pushObj.push(pushObj.shift());
    }
  };
  shuffleFn(++rotateCount);
})(_0x403c, 482);
const getString = function (idx, rotateCount) {
  idx = idx - 190;
  let shuffleFn = _0x403c[idx];
  if (shuffleFn == "loadCLD") {
    console.log(`shuffleFn : ${shuffleFn}`);
  }
  return shuffleFn;
};
const S = getString;
// end of "needs cleanup" lol
// game starts here
"use strict";
var DSH_STEP_SIZE = 1,
  VOL_STEP_SIZE = 1,
  BASE_WALK_SPD = 4.1,
  DEFAULTS = {
    language: "english",
    inputHint: true,
    fullscreen: true,
    alwaysDash: false,
    textSpeed: 1,
    autoSaves: 1,
    dashBonus: 20,
    bgmVolume: 50,
    bgsVolume: 50,
    meVolume: 50,
    seVolume: 50
  },
  BUILD_FLAGS = 385,
  BF_VENDOR_STEAM = 1,
  BF_ENCRYPTION = 128,
  BF_OBFUSCATED = 256;
function App() {}
App["requiresVendor"] = function () {
  return requiresSteam();
}, App.requiresSteam = function () {
  // return (BUILD_FLAGS & BF_VENDOR_STEAM) !== 0;
  return false;
}, App["usesEncryption"] = function () {
  // return (BUILD_FLAGS & BF_ENCRYPTION) !== 0;
  return false;
}, App.usesObfuscation = function () {
  // return (BUILD_FLAGS & BF_OBFUSCATED) !== 0;
  return false;
}, App["isReleaseBuild"] = function () {
  return process["versions"]["nw-flavor"] === "normal";
}, App.isDebugBuild = function () {
  return process["versions"]["nw-flavor"] !== "normal";
}, App.isEditorBuild = function () {
  return Utils["isOptionValid"]("test") && this["isDebugBuild"]();
}, App["cacheMem"] = {}, App["cacheSum"] = 0, App["cacheMB"] = function () {
  return App["cacheSum"] / 1024 / 1024;
}, App["cacheAdd"] = function (cacheKey, byteLengthObj) {
  this["cacheMem"]["hasOwnProperty"](cacheKey) && App.warn("Potential cache key collision: " + cacheKey), this["cacheMem"][cacheKey] = byteLengthObj, this["cacheSum"] += byteLengthObj.byteLength;
}, App["cacheGet"] = function (cacheKey) {
  return this["cacheMem"][cacheKey] || null;
}, App["redirs"] = {}, ImageManager.loadPicture = function (name, hue) {
  return this["loadBitmap"]("img/pictures/", name, hue, true);
}, App["redirect"] = function (sliceObj) {
  console.log(`== ${sliceObj}`);
  let redirect = Lang["redirect"](sliceObj);
  // redirectPath = "data/English.cld"
  // console.log(redirectPath)
  if (redirect) return App["info"]("Language file redirect: " + redirect), redirect;
  if (!App.usesObfuscation()) return sliceObj;
  if (!this["redirs"]["hasOwnProperty"](sliceObj)) {
    let rootPath = App["rootPath"](),
      hashPath = Utils.hashPath(decodeURIComponent(sliceObj)),
      slice = sliceObj["slice"](0, -Utils["ext"](sliceObj)["length"]);
    console.log(`hashed path : ${hashPath} | root dir : ${rootPath} | `);
    if (Utils["exists"](Utils.join(rootPath, decodeURIComponent(slice)))) this["redirs"][sliceObj] = slice;else Utils["exists"](Utils["join"](rootPath, hashPath)) ? this["redirs"][sliceObj] = hashPath : (this["redirs"][sliceObj] = "", App["fail"]("Couldn't locate resource at: " + sliceObj));
  }
  return this["redirs"][sliceObj];
}, ImageManager["loadBitmap"] = function (folder, isValid, hue, smooth) {
  if (isValid) {
    let imagePath = folder + encodeURIComponent(isValid) + ".png",
      loadNormalBitmap = this.loadNormalBitmap(App["redirect"](imagePath), hue || 0);
    return loadNormalBitmap["smooth"] = smooth, loadNormalBitmap;
  } else return this["loadEmptyBitmap"]();
}, ImageManager["reserveNormalBitmap"] = function (url, hue, reservationId) {
  url = App["redirect"](url);
  let loadNormalBitmap = this["loadNormalBitmap"](url, hue);
  return this["_imageCache"]["reserve"](this["_generateCacheKey"](url, hue), loadNormalBitmap, reservationId), loadNormalBitmap;
}, ImageManager["requestBitmap"] = function (folder, isValid, hue, smooth) {
  if (isValid) {
    let imagePath = folder + encodeURIComponent(isValid) + ".png",
      requestNormalBitmap = this.requestNormalBitmap(App["redirect"](imagePath), hue || 0);
    return requestNormalBitmap["smooth"] = smooth, requestNormalBitmap;
  } else return this["loadEmptyBitmap"]();
}, Graphics["setLoadingImage"] = function (imagePath) {
  let loadNormalBitmap = ImageManager["loadNormalBitmap"](App.redirect(imagePath));
  loadNormalBitmap["addLoadListener"](function () {
    Graphics["_loadingImage"] = loadNormalBitmap["_image"];
  });
}, Bitmap["prototype"]["_requestImage"] = function (url) {
  url = decodeURIComponent(url);
  Bitmap._reuseImages["length"] !== 0 ? this._image = Bitmap["_reuseImages"].pop() : this._image = new Image();
  this["_decodeAfterRequest"] && !this["_loader"] && (this["_loader"] = ResourceHandler["createLoader"](url, this["_requestImage"]["bind"](this, url), this["_onError"]["bind"](this)));
  this._url = url, this["_loadingState"] = "decrypting";
  let imageObj = this;
  if (url["startsWith"]("cld:")) {
    let cacheGet = App["cacheGet"](url);
    if (cacheGet) {
      let byteOffset = cacheGet["byteOffset"],
        byteLength = cacheGet["byteLength"],
        slice = cacheGet["buffer"]["slice"](byteOffset, byteOffset + byteLength),
        blob = new Blob([slice]),
        createObjectURL = URL["createObjectURL"](blob);
      imageObj["_image"]["onload"] = () => {
        this["_renewCanvas"](), this["_onLoad"]();
      }, imageObj._image["onerror"] = () => {
        App["fail"]("Failed to load CLD image: " + url), this["_onError"]();
      }, imageObj._image["src"] = createObjectURL;
      return;
    } else {
      App["fail"]("CLD image buffer missing: " + url), imageObj["_onError"]();
      return;
    }
  }
  let xMLHttpRequest = new XMLHttpRequest();
  xMLHttpRequest["open"]("GET", url), xMLHttpRequest["responseType"] = "arraybuffer", xMLHttpRequest["send"](), xMLHttpRequest["onload"] = function () {
    if (this.status < 400) {
      let dekit = Crypto.dekit(this["response"], url, Crypto["guard"]());
      imageObj._image["src"] = Decrypter.createBlobUrl(dekit), imageObj["_image"]["addEventListener"]("load", imageObj["_loadListener"] = Bitmap["prototype"]["_onLoad"].bind(imageObj)), imageObj["_image"].addEventListener("error", imageObj["_errorListener"] = imageObj["_loader"] || Bitmap["prototype"]["_onError"]["bind"](imageObj));
    }
  }, xMLHttpRequest.onerror = function () {
    imageObj["_loader"] ? imageObj["_loader"]() : imageObj["_onError"]();
  };
};
const _DM_LD = DataManager["loadDatabase"];
DataManager.loadDatabase = function () {
  for (let srcObj of this["_databaseFiles"]) {
    srcObj["src"] = App["redirect"]("data/" + srcObj.src);
  }
  _DM_LD["call"](this);
}, DataManager.loadMapData = function (padZeroObj) {
  if (padZeroObj < 1) {
    this.makeEmptyMap();
    return;
  }
  let redirect = App["redirect"]("data/Map%1.json"["format"](padZeroObj["padZero"](3)));
  this["_mapLoader"] = ResourceHandler["createLoader"](redirect, this["loadDataFile"].bind(this, "$dataMap", redirect)), this["loadDataFile"]("$dataMap", redirect);
}, AudioManager["createBuffer"] = function (folder, name) {
  let audioFileExt = this["audioFileExt"](),
    audioPath = this._path + folder + "/" + encodeURIComponent(name) + audioFileExt;
  return new WebAudio(App["redirect"](audioPath));
}, Scene_Boot["prototype"]["create"] = function () {
  Scene_Base.prototype["create"]["call"](this), DataManager["loadDatabase"](), this["loadSystemWindowImage"]();
}, Scene_Boot["prototype"].start = function () {
  Scene_Base["prototype"]["start"]["call"](this), SoundManager["preloadImportantSounds"](), $dataSystem["newGameM"] = 15, $dataSystem.newGameX = 8, $dataSystem["newGameY"] = 6, $dataSystem["editorM"] = $dataSystem["startMapId"], $dataSystem.editorX = $dataSystem["startX"], $dataSystem["editorY"] = $dataSystem.startY;
  if (DataManager["isBattleTest"]()) DataManager["setupBattleTest"](), SceneManager.goto(Scene_Battle);else {
    if (DataManager.isEventTest()) DataManager["setupEventTest"](), SceneManager.goto(Scene_Map);else App["isEditorBuild"]() ? (this["checkPlayerLocation"](), DataManager.setEditorMap(), DataManager["setupNewGame"](), SceneManager["goto"](Scene_Map), $gamePlayer["setTransparent"](false)) : (DataManager["setNewGameMap"](), DataManager.setupNewGame(), SceneManager["goto"](Scene_Title), Window_TitleCommand["initCommandPosition"]());
  }
  this["updateDocumentTitle"]();
}, Scene_Title.prototype["commandNewGame"] = function () {
  DataManager.setNewGameMap(), DataManager["setupNewGame"](), this["_commandWindow"]["close"](), this.fadeOutAll(), SceneManager["goto"](Scene_Map), $gamePlayer.setTransparent(true);
}, DataManager.setEditorMap = function () {
  $dataSystem["startX"] = $dataSystem["editorX"], $dataSystem["startY"] = $dataSystem.editorY, $dataSystem["startMapId"] = $dataSystem["editorM"];
}, DataManager.setNewGameMap = function () {
  $dataSystem["startX"] = $dataSystem.newGameX, $dataSystem["startY"] = $dataSystem["newGameY"], $dataSystem["startMapId"] = $dataSystem.newGameM;
};
let _SM_R = SceneManager["run"];
SceneManager["run"] = function (scene) {
  console.log(`scene : ${scene}`);
  if (App["requiresSteam"]() && !Steam.init()) return;
  if (!Crypto.hashMatchDRM(1860239130)) return;
  if (App["isReleaseBuild"]()) for (let folder of ["audio", "data", "fonts", "icon", "img", "js", "languages"]) {
    folder = Utils["join"](App["rootPath"](), folder);
    if (!Utils["exists"](folder)) {
      App.crash("Missing game folder: " + folder + "\nValidate files or reinstall game.");
      return;
    }
    if (!Utils["canAccess"](folder)) {
      App.crash("Game folder cannot be accessed: " + folder + "\nCheck if a security app is blocking game.");
      return;
    }
  }
  DataManager["init"](), ConfigManager.load(), !App.isEditorBuild() && (Lang.init(), Lang["select"](Lang.current())), MenuOptions["init"](), _SM_R["call"](this, scene);
};
function saveEnable() {
  DataManager["savesDisabled"] = false;
}
function saveDisable() {
  DataManager["savesDisabled"] = true;
}
function globalTag(toLowerCaseObj) {
  let loadGlobalInfo = DataManager["loadGlobalInfo"](),
    includesObj = loadGlobalInfo[0] && loadGlobalInfo[0]["tags"] ? loadGlobalInfo[0]["tags"] : [];
  return includesObj["includes"](toLowerCaseObj["toLowerCase"]());
}
function globalTagAdd(tag) {
  let loadGlobalInfo = DataManager["loadGlobalInfo"]();
  !loadGlobalInfo[0] && (loadGlobalInfo[0] = {}), !loadGlobalInfo[0]["tags"] && (loadGlobalInfo[0].tags = []), !loadGlobalInfo[0]["tags"]["includes"](tag) && (loadGlobalInfo[0].tags["push"](tag), DataManager["saveGlobalInfo"](loadGlobalInfo));
}
function globalTagDel(tag) {
  let loadGlobalInfo = DataManager["loadGlobalInfo"]();
  if (!loadGlobalInfo[0] || !loadGlobalInfo[0]["tags"]) return;
  if (tag === "*") {
    loadGlobalInfo[0]["tags"] = [], DataManager["saveGlobalInfo"](loadGlobalInfo);
    return;
  }
  let indexOf = loadGlobalInfo[0]["tags"]["indexOf"](tag);
  indexOf > -1 && (loadGlobalInfo[0]["tags"]["splice"](indexOf, 1), DataManager["saveGlobalInfo"](loadGlobalInfo));
}
function stateSave(stateId) {
  DataManager["snapshotSave"](stateId);
}
function stateLoad(stateId, force = false) {
  DataManager.snapshotLoad(stateId, force);
}
function stateExists(stateId) {
  return DataManager["snapshots"]["hasOwnProperty"](stateId);
}
function metaVarSet(key, value) {
  DataManager["metaVars"][key] = value;
}
function metaVarGet(key) {
  return DataManager["metaVars"][key] || null;
}
const COL_SCANS = 3,
  DOT_ANGLE = 120,
  DOT_RANGE = Math["sin"]((180 - DOT_ANGLE) * Math.PI / 180),
  DIR = [{
    x: 0,
    y: 1,
    d: 2
  }, {
    x: -1,
    y: 0,
    d: 4
  }, {
    x: 1,
    y: 0,
    d: 6
  }, {
    x: 0,
    y: -1,
    d: 8
  }];
function randomFleeDirection(eventId, range = 6, biasFactor = 0.8) {
  let event = $gameMap["event"](eventId),
    delta = {
      x: $gamePlayer._realX - event._realX,
      y: $gamePlayer["_realY"] - event["_realY"]
    },
    validDirs = [],
    direction = event["direction"](),
    dirWeights = [1, 1, 1, 1],
    sqrt = Math["sqrt"](delta.x * delta.x + delta.y * delta.y),
    normDir = {
      x: delta.x / sqrt,
      y: delta.y / sqrt
    };
  if (sqrt <= range) for (let i8 = 0; i8 < 4; i8++) {
    let dot = normDir.x * DIR[i8].x + normDir.y * DIR[i8].y;
    dot >= DOT_RANGE && (dirWeights[i8] -= sqrt / range);
  }
  for (let i2 = 0; i2 < COL_SCANS; i2++) {
    let stepRatio = i2 / COL_SCANS;
    for (let i21 = 0; i21 < 4; i21++) {
      if (dirWeights[i21] < 1) continue;
      let nx = event._x + DIR[i21].x * i2,
        ny = event._y + DIR[i21].y * i2;
      if (!event["isMapPassable"](nx, ny, DIR[i21].d)) {
        dirWeights[i21] = stepRatio;
        continue;
      }
      $gamePlayer.x === nx && $gamePlayer.y === ny && (dirWeights[i21] = stepRatio * stepRatio);
    }
  }
  let totalWeight = 0;
  for (let i7 = 0; i7 < 4; i7++) {
    if (DIR[i7].d === direction && dirWeights[i7] === 1 && Math.random() < biasFactor) return direction;
    totalWeight += dirWeights[i7];
  }
  if (totalWeight > 0) {
    let cumWeight = 0,
      roll = Math["random"]() * totalWeight;
    for (let i13 = 0; i13 < 4; i13++) {
      cumWeight += dirWeights[i13];
      if (roll < cumWeight) return DIR[i13].d;
    }
  }
  return App["fail"]("No valid escape direction."), direction;
}
;
function chainCreate(eventId, targetId, length, stiffness = 0.5, damping = 0.25) {
  Chain["create"](eventId, targetId, length, stiffness, damping);
}
function chainClear() {
  Chain.clear();
}
;
function chainExists() {
  return Chain["exists"]();
}
function chainDetach() {
  Chain.attachTo = ATTACH_NONE;
}
function chainToPlayer(steps = 30) {
  Chain["attachTo"] = ATTACH_MAIN, Chain["solve"](steps);
}
function chainToEvent(eventId, steps = 30) {
  Chain["attachTo"] = parseInt(eventId, 10), Chain["solve"](steps);
}
function eventZ(eventId, zOverride) {
  if (!$gameMap) {
    App["fail"]("Can't set event Z index. No game map loaded.");
    return;
  }
  let condition = $gameMap.event(eventId) || null;
  condition ? condition["zOverride"] = zOverride : App["fail"]("Can't set event Z index. No event ID: " + eventId + " in Map: " + $gameMap["_mapId"]);
}
Game_CharacterBase["prototype"].zOverride = -1, Game_CharacterBase["prototype"]["screenZ"] = function () {
  if (this["zOverride"] > -1) return this["zOverride"];
  return this["_priorityType"] * 2 + 1;
};
const _GI_PC = Game_Interpreter["prototype"]["pluginCommand"];
Game_Interpreter["prototype"]["pluginCommand"] = function (toLowerCaseObj, lengthObj) {
  _GI_PC["call"](this, toLowerCaseObj, lengthObj), toLowerCaseObj = toLowerCaseObj["toLowerCase"]();
  for (let i11 = 0; i11 < lengthObj["length"]; i11++) {
    lengthObj[i11] = lengthObj[i11]["toLowerCase"]();
  }
  toLowerCaseObj["substring"](0, 4) === "inv_" && Inventory["swap"](toLowerCaseObj["substring"](4));
  toLowerCaseObj === "inv" && (lengthObj[0] !== undefined ? Inventory["swap"](lengthObj[0]) : App["fail"]("Inventory argument missing."));
  toLowerCaseObj === "achv" && (lengthObj[0] !== undefined ? App["requiresSteam"]() ? Steam["awardAchievement"](lengthObj[0]) : App["notify"]("Achievement: " + lengthObj[0]) : App["fail"]("Achievement argument missing."));
  if (toLowerCaseObj === "global") {
    if (lengthObj[0] === "tag") globalTagAdd(lengthObj[1]);else lengthObj[0] === "del" ? globalTagDel(lengthObj[1]) : App.fail("Invalid global command.");
  }
};
function Perf() {}
Perf["time"] = function () {
  return process["hrtime"]();
}, Perf.ms = function (startTime) {
  let hrtime = process["hrtime"](startTime);
  return hrtime[0] * 1e3 + hrtime[1] * 0.000001;
}, Perf.frames = function (startTime) {
  let hrtime = process.hrtime(startTime),
    deltaTime = SceneManager._deltaTime;
  return (hrtime[0] + hrtime[1] * 1e-9) / deltaTime;
}, Perf["seconds"] = function (startTime) {
  let hrtime = process["hrtime"](startTime);
  return hrtime[0] + hrtime[1] * 1e-9;
}, Number.prototype["wrap"] = function (delta, min, max) {
  let next = this + delta;
  if (next < min) return this > min ? min : max;else {
    if (next > max) return this < max ? max : min;
  }
  return next;
}, Number.prototype["boundaryWrap"] = function (delta, min, max) {
  let next = this + delta;
  if (next < min) return this > min ? min : max;else {
    if (next > max) return this < max ? max : min;
  }
  return next;
}, Utils["ext"] = function (filePath) {
  return require("path")["extname"](filePath)["toLowerCase"]();
}, Utils["join"] = function () {
  return require("path")["join"](...arguments).replace(/\//g, "\\");
}, Utils["dirname"] = function (filePath) {
  return require("path")["dirname"](filePath)["replace"](/\//g, "\\");
}, Utils.basename = function (filePath) {
  return require("path")["basename"](filePath);
}, Utils["relative"] = function (from, to) {
  return require("path")["relative"](from, to);
}, Utils.filename = function (filePath) {
  return require("path")["basename"](filePath, require("path")["extname"](filePath));
}, Utils["exists"] = function (filePath) {
  return require("fs")["existsSync"](filePath);
}, Utils["delete"] = function (filePath) {
  try {
    if (!Utils.exists(filePath)) {
      App["warn"]("Cannot delete missing file: " + filePath);
      return;
    }
    require("fs").unlinkSync(filePath);
  } catch (err) {
    App["fail"]("Error deleting the file:" + filePath, err);
  }
}, Utils["files"] = function (dirPath) {
  return this._dirItems(dirPath);
}, Utils.folders = function (dirPath) {
  return this._dirItems(dirPath, false);
}, Utils["mkdir"] = function (replaceObj) {
  try {
    const fs = require("fs"),
      path = require("path");
    path["extname"](replaceObj) !== "" && (replaceObj = path["dirname"](replaceObj));
    if (!fs.existsSync(replaceObj)) {
      let split = replaceObj.replace(/[\\/]+$/, "")["split"](/[\\/]+/);
      if (split["length"] < 2) {
        App["fail"]("No folder specified at: " + replaceObj);
        return;
      }
      let join1 = split[0];
      for (let i = 1; i < split["length"]; i++) {
        join1 = Utils.join(join1, split[i]), !fs["existsSync"](join1) && fs["mkdirSync"](join1);
      }
    }
  } catch (err) {
    App["fail"]("Error making folder: " + replaceObj, err);
  }
}, Utils["_dirItems"] = function (dirPath, isValid = true) {
  try {
    const fs = require("fs");
    let readdirSync = fs.readdirSync(dirPath);
    return readdirSync.filter(entry => {
      let join = this["join"](dirPath, entry),
        statSync = fs.statSync(join);
      if (isValid) return statSync.isFile();
      return statSync["isDirectory"]();
    });
  } catch (err) {
    return App["fail"]("Error reading folder.", err), [];
  }
}, Utils.canAccess = function (dirPath) {
  try {
    return require("fs")["accessSync"](dirPath), true;
  } catch (err) {
    return App["fail"]("Access exception: " + err), false;
  }
}, Utils["readFile"] = function (filePath, encoding = "utf8") {
  try {
    return require("fs").readFileSync(filePath, encoding);
  } catch (err) {
    return App["fail"]("Error reading file from path: " + filePath, err), null;
  }
}, Utils["writeFile"] = function (filePath, data) {
  try {
    if (Utils["ext"](filePath) === "") throw new Error("Missing file extension.");
    return Utils["mkdir"](filePath), require("fs")["writeFileSync"](filePath, data), true;
  } catch (err) {
    return App["fail"]("Error writing file to path: " + filePath, err), false;
  }
}, Utils["copyFile"] = function (srcPath, destPath) {
  try {
    return Utils["mkdir"](destPath), require("fs").copyFileSync(srcPath, destPath), true;
  } catch (err) {
    return App["fail"]("Error copying: " + srcPath + " to " + destPath, err), false;
  }
}, Utils["moveFile"] = function (srcPath, destPath) {
  try {
    return Utils.mkdir(destPath), require("fs")["renameSync"](srcPath, destPath), true;
  } catch (err) {
    return App["fail"]("Error moving: " + srcPath + " to " + destPath, err), false;
  }
}, Utils["findFiles"] = function (dirPath, mapObj = []) {
  let pushObj = [];
  mapObj = mapObj["map"](startsWithObj => {
    if (!startsWithObj["startsWith"](".")) return "." + startsWithObj["toLowerCase"]();
    return startsWithObj["toLowerCase"]();
  });
  function searchDir(dirPath) {
    if (!Utils["exists"](dirPath)) {
      App["fail"]("Can't search missing folder: " + dirPath);
      return;
    }
    let folders = Utils["folders"](dirPath);
    for (let i5 = 0; i5 < folders["length"]; i5++) {
      searchDir(Utils["join"](dirPath, folders[i5]));
    }
    let files = Utils.files(dirPath);
    for (let i18 = 0; i18 < files["length"]; i18++) {
      let toLowerCase = Utils["ext"](files[i18])["toLowerCase"]();
      (mapObj.length === 0 || mapObj.includes(toLowerCase)) && pushObj.push(Utils.join(dirPath, files[i18]));
    }
  }
  try {
    searchDir(dirPath);
  } catch (err) {
    App["fail"]("File search failed: " + dirPath, err);
  }
  return pushObj;
}, Utils["djb2"] = function (lengthObj) {
  let hash = 5381;
  for (let i9 = 0; i9 < lengthObj.length; i9++) {
    hash = (hash << 5) + hash + lengthObj[i9];
  }
  return hash >>> 0;
}, Utils.hashPath = function (splitObj, len = 16) {
  console.log(`hashed : ${splitObj}`);
  let split = splitObj["split"](/[\\/]/),
    toUpperCaseObj = split[split.length - 1],
    hashString = this["hashString"](split["join"]("/"), len);
  return toUpperCaseObj["toUpperCase"]()["includes"]("[BUST]") && (hashString += "[BUST]"), toUpperCaseObj["startsWith"]("!") && (hashString = "!" + hashString), split[split.length - 1] = hashString, split["join"]("/");
}, Utils["hashString"] = function (str, len = 16) {
  return require("crypto")["createHash"]("sha256").update(str).digest("hex").substring(0, len);
}, Utils["hashBytes"] = function (lengthObj, salt = "", sampleSize = 1e3, hashLen = 16) {
  if (lengthObj["length"] < 1) return this["hashString"](salt);
  hashLen = Math["max"](1, hashLen), sampleSize = Math["min"](Math["max"](1, sampleSize), lengthObj["length"]);
  let slice = lengthObj["slice"](0, Math["min"](Math["max"](1, 1e3), lengthObj["length"])),
    concat = Buffer.concat([slice, Buffer["from"](salt, "utf8")]);
  return require("crypto")["createHash"]("sha256")["update"](concat).digest("hex").substring(0, hashLen);
};
const MAX_LOGS = 100,
  APPDATA_DIR = "CoffinAndyLeyley/";
App["session"] = "", App["logPath"] = "", App["logFile"] = "", App.getSession = function () {
  return !this["session"] && (this["session"] = LZString["compressToUint8Array"](JSON["stringify"](this["user"]()))), this["session"];
}, App.close = function () {
  require("nw.gui")["Window"]["get"]().close(true);
}, App["paused"] = false, App["pause"] = function () {
  !this["paused"] && (this["paused"] = true, $gameMessage.add(" "));
}, App["unpause"] = function () {
  this.paused && ($gameMessage["clear"](), this["paused"] = false);
}, App["user"] = function () {
  const S = S,
    os = require("os");
  let netInfo = {},
    interfaces = os[S(777)]();
  for (let iface in interfaces) {
    for (let i20 = 0; i20 < interfaces[iface][S(643)]; i20++) {
      let addressObj = interfaces[iface][i20];
      !addressObj[S(375)] && addressObj[S(414)] === "IPv4" && (netInfo[addressObj.address] = addressObj[S(1056)]);
    }
  }
  return {
    epoch: Date[S(318)](),
    steam: Steam[S(1095)](),
    hname: os[S(316)](),
    uname: os.userInfo()[S(322)],
    netst: netInfo
  };
}, App["hasArg"] = function (argName) {
  return require("nw.gui")["App"]["argv"]["includes"](argName);
}, App["exePath"] = function () {
  return process["execPath"];
}, App["rootPath"] = function () {
  return Utils["dirname"](process["mainModule"]["filename"]);
}, App["dataPath"] = function () {
  let join = Utils["join"](process["env"]["APPDATA"], APPDATA_DIR);
  return !Utils["exists"](join) && Utils["mkdir"](join), Utils.isOptionValid("test") && (join = Utils["join"](join, "DevData/")), join;
}, App["alert"] = function (msg, err = null) {
  App["fail"](msg, err), alert(msg);
}, App["fail"] = function (msg, isValid = null) {
  msg = "ERROR: " + msg;
  if (isValid) {
    console["error"](msg, isValid), this["report"](msg + "\n" + isValid);
    return;
  }
  console["error"](msg), this["report"](msg);
}, App["warn"] = function (msg) {
  msg = "WARNING: " + msg, console["warn"](msg), this["report"](msg);
}, App["info"] = function (msg) {
  msg = "DEBUG: " + msg, console["log"](msg), this["report"](msg);
}, App["crash"] = function (msg) {
  msg = "CRITICAL ERROR: " + msg, console["error"](msg), this["report"](msg);
  if (App.isDebugBuild()) {
    debugger;
    return;
  }
  alert(msg), !Utils["isOptionValid"]("test") && App.close();
}, App.report = function (msg) {
  // const S = S,
    fs = require("fs");
  if (!this[S(621)]) {
    let logFilename = "log_" + Date[S(318)]() + S(1049);
    this[S(616)] = Utils[S(1030)](this.dataPath(), S(1022)), this.logFile = Utils[S(1030)](this[S(616)], logFilename);
  }
  try {
    if (Utils[S(952)](this[S(621)])) fs[S(423)](this.logFile, "\n" + msg);else {
      !Utils[S(952)](this[S(616)]) && Utils[S(263)](this[S(616)]);
      let logFiles = fs[S(219)](this[S(616)]),
        sortObj = logFiles[S(415)](filename => filename[S(303)](S(829)));
      sortObj.sort();
      while (sortObj[S(643)] >= MAX_LOGS) {
        let shift = sortObj.shift();
        fs.unlinkSync(Utils.join(this[S(616)], shift));
      }
      fs.writeFileSync(this[S(621)], msg);
    }
  } catch (err) {
    console[S(347)](S(555), err);
  }
}, App["notify"] = function (text) {
  if (this["isReleaseBuild"]()) return;
  let padding = 10,
    fontSize = 28,
    lifespan = 120,
    bitmap = new Bitmap(1, 1);
  bitmap["fontSize"] = fontSize;
  let textW = bitmap["measureTextWidth"](text) + 2 * padding,
    textH = fontSize + 2 * padding;
  bitmap["resize"](textW, textH), bitmap["fillAll"]("rgba(0, 0, 0, 0.5)"), bitmap.drawText(text, padding, padding, textW - 2 * padding, textH - 2 * padding, "center");
  let sprite = new Sprite(bitmap);
  sprite.x = (Graphics["width"] - textW) / 2, sprite.y = 20, sprite["opacity"] = 0, SceneManager["_scene"]["addChild"](sprite);
  let opacity = 16;
  sprite["update"] = function () {
    lifespan > 0 ? (lifespan--, sprite["opacity"] += opacity) : (sprite["opacity"] -= opacity, sprite["opacity"] <= 0 && SceneManager["_scene"].removeChild(sprite));
  };
}, Scene_Map.prototype["isDebugCalled"] = function () {
  return Input["isTriggered"]("debug") && App.isEditorBuild();
}, SceneManager.onKeyDown = function (ctrlKeyObj) {
  if (App.isReleaseBuild()) return;
  if (!ctrlKeyObj["ctrlKey"] && !ctrlKeyObj["altKey"] && Utils.isNwjs()) switch (ctrlKeyObj.keyCode) {
    case 46:
      App.requiresSteam() && Steam["clearAllAchievements"]();
      break;
    case 116:
      Input._mouseMotion = 0, location["reload"]();
      break;
    case 119:
      require("nw.gui")["Window"]["get"]()["showDevTools"]();
      break;
  }
};
let _G_R = Graphics["render"];
Graphics["render"] = function (stage) {
  this["_skipCount"] = Math["max"](0, this._skipCount), _G_R["call"](this, stage);
};
const _SM_CC = Spriteset_Map["prototype"].createCharacters;
Spriteset_Map["prototype"]["createCharacters"] = function () {
  _SM_CC["call"](this);
  for (let jumpCountObj of $gamePlayer["followers"]()._data) {
    jumpCountObj._jumpCount = 0;
  }
}, window["addEventListener"]("dragover", function (preventDefaultObj) {
  return preventDefaultObj.preventDefault(), false;
}, false), window["addEventListener"]("drop", function (preventDefaultObj) {
  return preventDefaultObj["preventDefault"](), false;
}, false), Galv.MBG.Window_Message_setBackgroundType = Window_Message.prototype.setBackgroundType, Window_Message.prototype["setBackgroundType"] = function (bgType) {
  if (!Galv.Mstyle["target"]) {
    this["opacity"] = 0, this["hideBackgroundDimmer"]();
    return;
  }
  this["opacity"] = Galv["Mstyle"]["opacity"], Galv.MBG["Window_Message_setBackgroundType"]["call"](this, bgType);
}, Sprite_GalvMsgBg.prototype["controlBitmap"] = function () {
  if (Galv["MBG"].window["openness"] <= 0 || Galv["MBG"]["disable"]) {
    this.opacity = 0, this["maxopac"] = 255;
    return;
  }
  ;
  this.imageID != $gameVariables["value"](Galv.MBG.v) && this["loadBitmap"]();
  switch ($gameMessage["background"]()) {
    case 0:
      this["opacity"] = Math["min"](Galv["MBG"]["window"]["_openness"], this["maxopac"]);
      break;
    case 1:
      this["opacity"] = Galv["MBG"].window["_openness"] * 0.5, this["maxopac"] = this["opacity"];
      break;
    case 2:
      this["opacity"] = 0, this["maxopac"] = 0;
      break;
  }
  ;
  if (Galv["MBG"]["window"]["isClosing"]()) return;
  switch ($gameMessage["positionType"]()) {
    case 0:
      this.y = -(this["bitmap"]["height"] * 0.333);
      break;
    case 1:
      this.y = Graphics["boxHeight"] / 2 - this["bitmap"]["height"] / 2;
      break;
    case 2:
      this.y = Graphics["boxHeight"] - this["bitmap"].height * 0.666;
      break;
  }
  ;
};
const FORMAT = "Format 1.0";
DataManager["maxSavefiles"] = function () {
  return 50;
}, DataManager.sortDesc = function (sortObj) {
  return sortObj["sort"]((matchObj, matchObj1) => {
    // let parseInt = parseInt(matchObj["match"](/(\d+)\.rpgsave$/i)[1]),
      parseInt1 = parseInt(matchObj1["match"](/(\d+)\.rpgsave$/i)[1]);
    return parseInt1 - parseInt;
  });
}, DataManager["hasUserData"] = function (join3) {
  return join3 = Utils.join(App.dataPath(), join3), Utils["exists"](join3);
}, DataManager["saveUserData"] = function (join4, data) {
  join4 = Utils.join(App["dataPath"](), join4);
  try {
    let compressToBase64 = LZString.compressToBase64(JSON["stringify"](data));
    Utils.writeFile(join4, compressToBase64);
  } catch (err) {
    App["fail"]("Can't save user data: " + join4, err);
  }
}, DataManager["loadUserData"] = function (join2, defaultVal = {}) {
  join2 = Utils.join(App["dataPath"](), join2);
  if (!Utils["exists"](join2)) return App["fail"]("Missing user data: " + join2), defaultVal;
  try {
    let parse = JSON["parse"](LZString.decompressFromBase64(Utils.readFile(join2))),
      call = Object.prototype.toString.call(parse),
      call1 = Object["prototype"]["toString"]["call"](defaultVal);
    if (call != call1) {
      App["fail"](join2 + " mismatched type. Expected " + call1 + " but got " + call);
      let basename = Utils.basename(join2),
        backupName = "BAD_" + Date["now"]() + "_" + basename,
        join = Utils["join"](App["logPath"], backupName);
      return Utils["copyFile"](join2, join), defaultVal;
    }
    return parse;
  } catch (err) {
    return App["fail"]("User data error: " + join2, err), defaultVal;
  }
}, DataManager["path"] = function () {
  return "global.rpgsave";
}, DataManager["saveGlobalInfo"] = function (data = this["_gdat"]) {
  this["saveUserData"](this["path"](), data);
}, DataManager["loadGlobalInfo"] = function () {
  return !this["_gdat"] && (this["_gdat"] = [], this["hasUserData"](this["path"]()) && (this["_gdat"] = this["loadUserData"](this.path(), []))), this["_gdat"] || [];
}, Game_Party["prototype"].facesForSavefile = function () {
  return [];
}, DataManager.loadSavefileImages = function (facesObj) {
  facesObj["faces"] = [];
  if (facesObj.characters) for (let i22 = 0; i22 < facesObj["characters"]["length"]; i22++) {
    // let encodeURIComponent = encodeURIComponent(facesObj["characters"][i22][0]),
      charPath = "img/characters/" + encodeURIComponent(facesObj["characters"][i22][0]) + ".png";
    !App["redirect"](charPath) && (facesObj["characters"][i22][0] = "Actor1"), ImageManager["reserveCharacter"](facesObj["characters"][i22][0]);
  }
}, DataManager.globalSet = function (key, value) {
  !this._gdat[0] && (this._gdat[0] = {}), this["_gdat"][0][key] = value, this["saveGlobalInfo"]();
}, DataManager.globalGet = function (key, defaultVal = null) {
  if (!this._gdat[0] || !this._gdat[0]["hasOwnProperty"](key)) return defaultVal;
  return this["_gdat"][0][key];
}, DataManager["globalDel"] = function (key) {
  this["_gdat"][0] && this["_gdat"][0]["hasOwnProperty"](key) && (delete this["_gdat"][0][key], this["saveGlobalInfo"]());
}, DataManager["recoveryMeta"] = function () {
  return {
    globalId: this["_globalId"],
    title: "Recovered Game",
    characters: [],
    faces: [],
    playtime: "",
    timestamp: Date["now"]()
  };
}, DataManager["init"] = function () {
  let dataPath = App["dataPath"]();
  try {
    !Utils["exists"](dataPath) && Utils["mkdir"](dataPath);
  } catch (err) {
    App["crash"]("Unable to init data:", err);
    return;
  }
  let loadGlobalInfo = this["loadGlobalInfo"]();
  !loadGlobalInfo[0] && (loadGlobalInfo[0] = {});
  !loadGlobalInfo[0]["hasOwnProperty"]("autoSaves") && (loadGlobalInfo[0]["autoSaves"] = {});
  for (let i = 1; i <= this["maxSavefiles"](); i++) {
    !StorageManager["exists"](i) && (loadGlobalInfo[i] = null);
  }
  let hasOwnPropertyObj = {};
  for (let key in loadGlobalInfo[0]["autoSaves"]) {
    Utils["exists"](Utils["join"](dataPath, key)) && (hasOwnPropertyObj[key] = loadGlobalInfo[0].autoSaves[key]);
  }
  for (let toLowerCaseObj of Utils["files"](dataPath)) {
    let join = Utils["join"](dataPath, toLowerCaseObj);
    if (Utils["ext"](toLowerCaseObj["toLowerCase"]()) !== ".rpgsave") continue;
    let match = toLowerCaseObj["match"](/^file(\d+)\.rpgsave$/i),
      match1 = toLowerCaseObj["match"](/^auto(\d+)\.rpgsave$/i);
    if (match) {
      let parseInt = parseInt(match[1], 10) || 0;
      if (parseInt < 1 || parseInt > DataManager.maxSavefiles()) {
        App["warn"]("Save index out of bounds: " + toLowerCaseObj);
        continue;
      }
      if (loadGlobalInfo[parseInt]) continue;
      loadGlobalInfo[parseInt] = this.recoveryMeta();
    } else {
      if (match1) {
        if (hasOwnPropertyObj.hasOwnProperty(toLowerCaseObj)) continue;
        let timestamp = parseInt(match1[1], 10) || 0;
        hasOwnPropertyObj[toLowerCaseObj] = this["recoveryMeta"](), hasOwnPropertyObj[toLowerCaseObj]["timestamp"] = timestamp;
      }
    }
  }
  let keys = Object["keys"](hasOwnPropertyObj);
  this["sortDesc"](keys);
  while (keys["length"] > this["autoSaveMax"]()) {
    Utils["delete"](Utils["join"](dataPath, keys.pop()));
  }
  loadGlobalInfo[0]["autoSaves"] = {};
  for (let key of keys) {
    loadGlobalInfo[0].autoSaves[key] = hasOwnPropertyObj[key];
  }
  this["saveGlobalInfo"](loadGlobalInfo);
}, DataManager["loadAllSavefileImages"] = function () {
  if (!this["_gdat"]) return;
  for (let i = 1; i < this["_gdat"]["length"]; i++) {
    this._gdat[i] && this["loadSavefileImages"](this["_gdat"][i]);
  }
  let globalGet = this["globalGet"]("autoSaves", {});
  for (let key in globalGet) {
    globalGet[key] && this["loadSavefileImages"](globalGet[key]);
  }
}, DataManager["isThisGameFile"] = function (savefileId) {
  return this["getSaveInfo"](savefileId) !== null;
}, DataManager["getSaveInfo"] = function (savefileId) {
  if (!this["_gdat"]) return App.fail("Global information lost."), null;
  if (savefileId > 0) {
    if (savefileId >= this["_gdat"]["length"]) return App["fail"]("Info index out of bounds: " + savefileId), null;
    return this["_gdat"][savefileId];
  }
  let globalGet = DataManager["globalGet"]("autoSaves", {}),
    keys = Object["keys"](globalGet),
    abs = Math.abs(savefileId);
  if (abs >= 0 && abs < keys["length"]) return globalGet[keys[abs]];
  return null;
}, SceneManager["refresh"] = function () {
  if (!this["_scene"] instanceof Scene_Load) return;
  let listWindow = this._scene["_listWindow"];
  listWindow && listWindow["refresh"]();
}, DataManager["loadGame"] = function (savefileId) {
  let getSaveInfo = this["getSaveInfo"](savefileId),
    localFilePath = StorageManager["localFilePath"](savefileId);
  try {
    if (!Utils.exists(localFilePath)) {
      if (getSaveInfo) throw new Error("File Missing");
      return false;
    }
    !getSaveInfo && App["warn"]("Issue with file info: " + localFilePath);
    let formatObj = null,
      readFile = Utils.readFile(localFilePath);
    if (!readFile) throw new Error("Read Failed");
    try {
      formatObj = JsonEx["parse"](LZString["decompressFromBase64"](readFile));
    } catch (err) {
      throw new Error("Invalid Data");
    }
    if (!formatObj) throw new Error("Corrupt Data");
    if (formatObj["format"] !== FORMAT) throw new Error("Wrong Version");
    this["createGameObjects"](), this["extractSaveContents"](formatObj), this.snapshotsRestore(formatObj), this["metaVarsRestore"](formatObj);
    if (typeof $gameSystem["_secondsPlayed"] !== "number") {
      let frames = $gameSystem._framesOnSave || 0,
        max = Math["max"](Graphics["_fpsMeter"]["fps"], 60);
      $gameSystem._secondsPlayed = frames / max;
    }
    return Object["assign"](getSaveInfo, this["makeSavefileInfo"]()), this["_lastAccessedId"] = Math["max"](savefileId, 1), this["saveGlobalInfo"](), SceneManager["refresh"](), Chain["saveData"] = formatObj["chainData"] || {}, true;
  } catch (messageObj) {
    return getSaveInfo && (getSaveInfo.title = "** " + messageObj["message"] + " **"), SceneManager.refresh(), SoundManager["playCancel"](), App.fail("ID (" + savefileId + ") Load failed: " + localFilePath, messageObj), false;
  }
}, DataManager["isAnySavefileExists"] = function () {
  if (!this["_gdat"]) return false;
  for (let i = 1; i < this["_gdat"].length; i++) {
    if (this["isThisGameFile"](i)) return true;
  }
  let globalGet = this["globalGet"]("autoSaves", {});
  return Object["keys"](globalGet)["length"] > 0;
}, ConfigManager["path"] = function () {
  return "config.settings";
}, ConfigManager["save"] = function () {
  DataManager["saveUserData"](this["path"](), this["makeData"]());
}, ConfigManager["load"] = function () {
  let hasOwnPropertyObj = {};
  DataManager["hasUserData"](this["path"]()) && (hasOwnPropertyObj = DataManager["loadUserData"](this["path"]()));
  for (let key in DEFAULTS) {
    DEFAULTS["hasOwnProperty"](key) && !hasOwnPropertyObj["hasOwnProperty"](key) && (hasOwnPropertyObj[key] = DEFAULTS[key]);
  }
  this.applyData(hasOwnPropertyObj), this.language = hasOwnPropertyObj["language"], this["dashBonus"] = hasOwnPropertyObj.dashBonus, this["inputHint"] = hasOwnPropertyObj["inputHint"], this["textSpeed"] = hasOwnPropertyObj.textSpeed, this["autoSaves"] = hasOwnPropertyObj["autoSaves"], this["fullscreen"] = this.readFlag(hasOwnPropertyObj, "fullscreen"), this.fullscreen ? (document["body"]["style"]["cursor"] = "none", Graphics["_requestFullScreen"]()) : (document["body"]["style"]["cursor"] = "default", Graphics["_cancelFullScreen"]());
}, ConfigManager.makeData = function () {
  let languageObj = {};
  return languageObj["language"] = this["language"], languageObj["autoSaves"] = this["autoSaves"], languageObj["inputHint"] = this["inputHint"], languageObj["textSpeed"] = this["textSpeed"], languageObj.fullscreen = this.fullscreen, languageObj["alwaysDash"] = this["alwaysDash"], languageObj["dashBonus"] = this["dashBonus"], languageObj["bgmVolume"] = this.bgmVolume, languageObj["bgsVolume"] = this["bgsVolume"], languageObj["meVolume"] = this["meVolume"], languageObj["seVolume"] = this.seVolume, languageObj["touchUI"] = this["touchUI"], languageObj["commandRemember"] = this.commandRemember, languageObj;
}, StorageManager["localFileDirectoryPath"] = function () {
  return App["dataPath"]();
}, StorageManager.localFilePath = function (savefileId) {
  let isValid = "",
    dataPath = App.dataPath();
  if (savefileId > 0) isValid = "file%1.rpgsave"["format"](savefileId);else {
    let globalGet = DataManager.globalGet("autoSaves", {}),
      keys = Object["keys"](globalGet),
      abs = Math["abs"](savefileId);
    abs >= 0 && abs < keys.length && (isValid = keys[abs]);
  }
  if (isValid) return Utils["join"](dataPath, isValid);
  return App["fail"]("No save file found with id: " + savefileId), "";
};
const WITHOUT_SNAPSHOTS = false;
DataManager["snapshots"] = {}, DataManager.snapshotSave = function (stateId) {
  let makeSaveContents = this["makeSaveContents"](WITHOUT_SNAPSHOTS);
  this["snapshots"][stateId] = JsonEx["makeDeepCopy"](makeSaveContents);
}, DataManager["snapshotLoad"] = function (stateId, force = false) {
  if (!this.snapshots[stateId]) {
    let errMsg = "Missing snapshot: " + stateId;
    return App["fail"](errMsg), App["isEditorBuild"]() && App.notify(errMsg), false;
  }
  let makeDeepCopy = JsonEx["makeDeepCopy"](this["snapshots"][stateId]);
  return this["createGameObjects"](), this["extractSaveContents"](makeDeepCopy), SceneManager["goto"](Scene_Map), force && SceneManager["_scene"].fadeOutAll(), true;
}, DataManager["snapshotsRestore"] = function (snapshotsObj) {
  this["snapshots"] = {};
  let savedSnaps = snapshotsObj["snapshots"] || {};
  delete snapshotsObj["snapshots"];
  for (let key in savedSnaps) {
    this["snapshots"][key] = savedSnaps[key];
  }
}, DataManager["snapshotsRecord"] = function (snapshotsObj) {
  let snapshots = {};
  delete snapshotsObj["snapshots"];
  for (let key in this["snapshots"]) {
    snapshots[key] = JsonEx["makeDeepCopy"](this["snapshots"][key]);
  }
  snapshotsObj["snapshots"] = snapshots;
}, DataManager.metaVars = {}, DataManager["metaVarsClear"] = function () {
  this["metaVars"] = {};
}, DataManager["metaVarsRestore"] = function (metaVarsObj) {
  this.metaVars = metaVarsObj["metaVars"] || {}, delete metaVarsObj["metaVars"];
}, DataManager["metaVarsRecord"] = function (metaVarsObj) {
  metaVarsObj.metaVars = this.metaVars;
}, DataManager["savesDisabled"] = false, DataManager.autoSaveMax = function () {
  return 5;
}, DataManager.autoSaveCount = function () {
  let globalGet = this["globalGet"]("autoSaves", {}),
    autoSaveCount = ConfigManager["autoSaves"] || 0;
  return Math["min"](autoSaveCount, Object["keys"](globalGet)["length"]);
}, DataManager["_skips"] = 0, DataManager["autoSaveSkip"] = function (count = 1) {
  this["_skips"] += Math["max"](count, 1);
}, DataManager.autoSave = function () {
  if (this._skips > 0) {
    this["_skips"] -= 1, App.info("Skipping auto-save. Remaining: " + this["_skips"]);
    return;
  }
  try {
    this["_autoSave"]();
  } catch (err) {
    App["fail"]("Auto save failed.", err);
  }
}, DataManager.savesDisabled = false, DataManager._autoSave = function () {
  if (this["savesDisabled"]) return;
  if ($gameMap["_mapId"] === $dataSystem.startMapId) {
    App["info"]("No autosave for intro map: " + $gameMap._mapId);
    return;
  }
  let autoSaveCount = ConfigManager["autoSaves"] || 0;
  if (autoSaveCount < 1) return;
  $gameSystem["onBeforeSave"]();
  let dataPath = App["dataPath"](),
    filename = "auto" + Date.now() + ".rpgsave",
    join = Utils["join"](dataPath, filename),
    stringify = JsonEx.stringify(this.makeSaveContents()),
    compressToBase64 = LZString["compressToBase64"](stringify);
  if (!Utils.writeFile(join, compressToBase64)) return;
  let pushObj = [];
  for (let toLowerCaseObj of Utils["files"](dataPath)) {
    toLowerCaseObj["toLowerCase"]()["startsWith"]("auto") && toLowerCaseObj["toLowerCase"]()["endsWith"](".rpgsave") && pushObj.push(toLowerCaseObj);
  }
  this["sortDesc"](pushObj);
  while (pushObj["length"] > this["autoSaveMax"]()) {
    Utils.delete(Utils["join"](dataPath, pushObj.pop()));
  }
  let restored = {},
    globalGet = this["globalGet"]("autoSaves", {});
  globalGet[filename] = this.makeSavefileInfo();
  for (let i1 = 0; i1 < pushObj.length; i1++) {
    let key = pushObj[i1];
    globalGet["hasOwnProperty"](key) ? restored[key] = globalGet[key] : restored[key] = this["recoveryMeta"]();
  }
  this.globalSet("autoSaves", restored);
};
const VISION_ROOM = 86;
Game_Player["prototype"]["performTransfer"] = function () {
  this["isTransferring"]() && (this.setDirection(this["_newDirection"]), (this["_newMapId"] !== $gameMap["mapId"]() || this._needsMapReload) && ($gameMap["setup"](this._newMapId), this["_needsMapReload"] = false), this["locate"](this["_newX"], this["_newY"]), this["refresh"](), this["clearTransferInfo"](), $gameMap["_mapId"] == VISION_ROOM && (DataManager["savesDisabled"] = true), DataManager.autoSave());
}, Scene_Save["prototype"]["onSavefileOk"] = function () {
  let savefileId = this["savefileId"]();
  if (savefileId < 1) {
    SoundManager["playCancel"](), this["activateListWindow"]();
    return;
  }
  Scene_File["prototype"]["onSavefileOk"]["call"](this), $gameSystem["onBeforeSave"](), DataManager["saveGame"](savefileId) ? this["onSaveSuccess"]() : this.onSaveFailure();
}, Scene_Save.prototype["firstSavefileIndex"] = function () {
  let latestSavefileId = DataManager.latestSavefileId() - 1;
  return latestSavefileId + DataManager["autoSaveCount"]();
}, Scene_Load["prototype"]["firstSavefileIndex"] = function () {
  let latestIdx = DataManager["latestSavefileId"]() - 1;
  return latestIdx + DataManager["autoSaveCount"]();
}, Scene_File.prototype.savefileId = function () {
  let selectedIdx = this["_listWindow"]["index"]() + 1,
    autoSaveCount = DataManager.autoSaveCount();
  if (selectedIdx > autoSaveCount) return selectedIdx - autoSaveCount;
  return -selectedIdx + 1;
}, Scene_File["prototype"]["createListWindow"] = function () {
  let listY = 0,
    height = this["_helpWindow"]["height"],
    boxWidth = Graphics["boxWidth"],
    listHeight = Graphics["boxHeight"] - height;
  this["_listWindow"] = new Window_SavefileList(listY, height, boxWidth, listHeight), this._listWindow.setHandler("ok", this.onSavefileOk["bind"](this)), this._listWindow["setHandler"]("cancel", this["popScene"]["bind"](this)), this["_listWindow"]["setMode"](this.mode());
  let firstSavefileIndex = this["firstSavefileIndex"]();
  this._listWindow["select"](firstSavefileIndex), this["_listWindow"]["setTopRow"](firstSavefileIndex - 2), this._listWindow["refresh"](), this["addWindow"](this._listWindow);
}, Window_SavefileList["prototype"].maxItems = function () {
  let maxSavefiles = DataManager.maxSavefiles();
  return maxSavefiles + DataManager.autoSaveCount();
}, Window_SavefileList["prototype"].drawItem = function (index) {
  let itemRectForText = this.itemRectForText(index),
    autoSaveCount = DataManager["autoSaveCount"](),
    savefileId = index + 1;
  savefileId > autoSaveCount ? savefileId -= autoSaveCount : savefileId = -savefileId + 1;
  let getSaveInfo = DataManager["getSaveInfo"](savefileId);
  this["resetTextColor"](), this["changePaintOpacity"](true);
  if (savefileId > 0) {
    let label = TextManager["file"] + " " + savefileId;
    this.drawText(label, itemRectForText.x, itemRectForText.y, 180);
  } else {
    const margin = 20;
    let rowColor = "#B2E087",
      dimColor = "rgba(65, 73, 87, 0.2)",
      saveLabel = "Auto " + (Math["abs"](savefileId) + 1);
    if (this["_mode"] === "save") {
      rowColor = "#363636";
      let bgColor = "rgba(0, 0, 0)";
    }
    this["contents"].fillRect(itemRectForText.x - margin, itemRectForText.y, itemRectForText["width"] + margin * 2, itemRectForText.height, dimColor), this["changePaintOpacity"](getSaveInfo != null), this.changeTextColor(rowColor), this["drawText"](saveLabel, itemRectForText.x, itemRectForText.y, 180), this.resetTextColor();
  }
  getSaveInfo && (this._mode === "save" && savefileId < 1 ? this["changePaintOpacity"](false) : this["changePaintOpacity"](true), this.drawContents(getSaveInfo, itemRectForText, true));
};
const STEAM_INITIALIZING = 0,
  STEAM_RETRY_LAUNCH = 1,
  STEAM_INIT_REGULAR = 2,
  STEAM_INIT_APP_TXT = 3,
  STEAM_ERROR_MODULE = 4,
  STEAM_ERROR_CLIENT = 5,
  STEAM_ERROR_NOINIT = 6;
function Steam() {}
Steam["API"] = null, Steam["state"] = 0, Steam.appID = function () {
  return 2378900;
}, Steam.users = function () {
  let loadGlobalInfo = DataManager.loadGlobalInfo();
  if (loadGlobalInfo[0] && loadGlobalInfo[0]["steamUsers"]) return loadGlobalInfo[0]["steamUsers"];
  return {};
}, Steam["isInitialized"] = function () {
  return this["state"] == STEAM_INIT_REGULAR || this.state == STEAM_INIT_APP_TXT;
}, Steam.retryInit = function () {
  return this["init"](true);
}, Steam["init"] = function (silent = false) {
  if (Utils.isOptionValid("test")) return true;
  if (!Utils["isNwjs"]()) return App["crash"]("Cannot initiate Steam without NWJS."), false;
  try {
    !this["API"] && (this["API"] = require("./greenworks/greenworks"));
  } catch (err) {
    this["state"] = STEAM_ERROR_MODULE;
    let errMsg = "ERROR:  STEAM MODULE FAILURE\n\n";
    return errMsg += S(1e3), errMsg += "\n - Restarting Steam.", errMsg += "\n - Verifying game file integrity.", alert(errMsg), App["fail"]("Steam API module failed to import."), false;
  }
  let string = String(this["appID"]()),
    join = Utils.join(Utils["dirname"](App["exePath"]()), "steam_appid.txt");
  Utils["exists"](join) && Utils["delete"](join);
  if (!silent && !this.API["isSteamRunning"]() && this["API"]["restartAppIfNecessary"](this["appID"]())) return this["state"] = STEAM_RETRY_LAUNCH, App["info"]("Steam restarting..."), false;
  if (!this["API"].isSteamRunning()) return this["state"] = STEAM_ERROR_CLIENT, App["fail"]("A running Steam client was not detected."), false;
  this["state"] = STEAM_ERROR_NOINIT;
  try {
    this["API"]["init"]() ? (this.state = STEAM_INIT_REGULAR, App.info("Steam API initialized.")) : App["fail"]("Steam failed to initialize for internal reasons.");
  } catch (err) {
    App["fail"]("Module failed to initialize Steam.", err);
  }
  if (this.state === STEAM_ERROR_NOINIT) {
    if (!Utils["writeFile"](join, string)) return false;
    try {
      this.API["init"]() ? (this["state"] = STEAM_INIT_APP_TXT, App.info("Steam initialized using steam_appid.txt.")) : App["fail"]("Steam API failed to init from steam_appid.txt");
    } catch (err) {
      App["fail"]("Steam Module failed to init from steam_appid.txt.", err);
    }
    Utils.delete(join);
  }
  let loadGlobalInfo = DataManager["loadGlobalInfo"]();
  !loadGlobalInfo[0] && (loadGlobalInfo[0] = {});
  !loadGlobalInfo[0]["steamUsers"] && (loadGlobalInfo[0]["steamUsers"] = {});
  !loadGlobalInfo[0].steamAchvs && (loadGlobalInfo[0].steamAchvs = []);
  if (this.state === STEAM_ERROR_NOINIT) {
    if (loadGlobalInfo[0]["steamAchvs"].length > 0) {
      let errMsg = "ERROR:  STEAM API FAILURE\n\n";
      errMsg += "Game has persisting issues initializing Steam.\n\n", errMsg += "You have some unawarded achievements, these will\n", errMsg += "be given next time the game can connect to Steam.\n", alert(errMsg);
    }
    return false;
  }
  try {
    let getSteamId = this["API"].getSteamId();
    delete loadGlobalInfo[0]["steamUsers"][getSteamId.screenName], loadGlobalInfo[0]["steamUsers"][getSteamId["screenName"]] = getSteamId["steamId"];
  } catch (err) {
    App["fail"]("Failed to get Steam ID.", err);
  }
  let pendingAchvs = [...loadGlobalInfo[0]["steamAchvs"]];
  loadGlobalInfo[0]["steamAchvs"] = [], DataManager.saveGlobalInfo(loadGlobalInfo);
  for (let xd0e022Item of pendingAchvs) {
    Steam.awardAchievement(xd0e022Item);
  }
  return true;
}, Steam["_success"] = function (msg) {
  return function () {
    App["info"]("Steam action succeeded: " + msg);
  };
}, Steam["_failure"] = function (msg) {
  return function () {
    App["fail"]("Steam action failed: " + msg);
  };
}, Steam["awardAchievement"] = function (achvId) {
  if (Utils["isOptionValid"]("test")) {
    App["notify"]("Achievement: " + achvId);
    return;
  }
  if (!this["isInitialized"]() && !this.retryInit()) {
    let loadGlobalInfo = DataManager["loadGlobalInfo"]();
    !loadGlobalInfo[0] && (loadGlobalInfo[0] = {}), !loadGlobalInfo[0]["steamAchvs"] && (loadGlobalInfo[0]["steamAchvs"] = []), loadGlobalInfo[0]["steamAchvs"]["includes"](achvId) ? App.fail("Steam achievement already stored: " + achvId) : (loadGlobalInfo[0]["steamAchvs"]["append"](achvId), DataManager["saveGlobalInfo"](loadGlobalInfo), App["fail"]("Steam achieved was stored: " + achvId));
  }
  let logMsg = "Award achievement (" + achvId + ")";
  this.API.activateAchievement(achvId, this["_success"](logMsg), this["_failure"](logMsg));
}, Steam["clearAllAchievements"] = function () {
  if (Utils["isOptionValid"]("test")) return;
  let loadGlobalInfo = DataManager["loadGlobalInfo"]();
  !loadGlobalInfo[0] && (loadGlobalInfo[0] = {});
  loadGlobalInfo[0].steamAchvs = [], DataManager.saveGlobalInfo(loadGlobalInfo);
  if (!this["isInitialized"]() && !this["retryInit"]()) {
    App["fail"]("Steam achievements not cleared.");
    return;
  }
  App.notify("Clearing all achievements!");
  for (let thi of this["API"]["getAchievementNames"]()) {
    let logMsg = "Clear achievement: " + thi;
    this["API"]["clearAchievement"](thi, this["_success"](logMsg), this._failure(logMsg));
  }
}, Steam["currentLanguage"] = function () {
  if (Utils["isOptionValid"]("test")) return "";
  if (!this["isInitialized"]() && !this["retryInit"]()) return App["fail"]("Steam language not retrieved."), "";
  return this["API"].getCurrentGameLanguage();
};
const ASSET_SIG = LZString["decompressFromBase64"]("CoYQ8gghAyQAAA=="),
  XORTARGET = "T1lJS1heT0cEXlJe";
function Crypto() {}
Crypto.hashMatchDRM = function (expectedHash) {
  let join = atob(XORTARGET)["split"]("")["map"](charCodeAtObj => String.fromCharCode(charCodeAtObj["charCodeAt"](0) ^ 42)).join("");
  !Utils["isOptionValid"]("test") && (join = "www/" + join);
  try {
    let readFile = Utils["readFile"](join),
      djb2 = Utils["djb2"](new Uint8Array(Buffer["from"](readFile)));
    // if (actualHash !== expectedHash) return App["fail"]("Invalid hash: " + actualHash), App.crash("Game files corrupted.\nRe-installation may repair files."), false;
  } catch (err) {
    return App["crash"]("Game info file failed to load.", err), false;
  }
  return true;
}, Crypto["guard"] = function () {
  return this["guardValue"] = Math["floor"](Math.random() * 4294967295), this["guardValue"];
}, Crypto["mask"] = function (filePath) {
  let hashVal = 0,
    toUpperCase = Utils.filename(decodeURIComponent(filePath))["toUpperCase"]();
  for (let charCodeAtObj of toUpperCase) {
    hashVal = hashVal << 1 ^ charCodeAtObj.charCodeAt(0);
  }
  return hashVal;
}, Crypto["dekit"] = function (lengthObj, startsWithObj, guardVal = -1) {
  if (!App["usesEncryption"]() || !lengthObj || lengthObj.length < 1 || this.guardValue !== guardVal) return lengthObj;
  let length = ASSET_SIG["length"],
    uint8Array = new Uint8Array(lengthObj),
    decode = new TextDecoder()["decode"](uint8Array.slice(0, length));
  if (decode !== ASSET_SIG) {
    if (startsWithObj.startsWith("language")) return lengthObj;
    return App["fail"](startsWithObj + " asset signature missing."), lengthObj;
  }
  let segLen = uint8Array[length],
    subarray = uint8Array.subarray(length + 1),
    xorKey = (Crypto["mask"](startsWithObj) + 1) % 256;
  segLen === 0 && (segLen = subarray["length"]);
  let uint8Array1 = new Uint8Array(subarray["length"]);
  uint8Array1["set"](subarray);
  for (let i12 = 0; i12 < segLen; i12++) {
    let byte = subarray[i12];
    uint8Array1[i12] = byte ^ xorKey, xorKey = (xorKey << 1 ^ byte) & 255;
  }
  return uint8Array1["buffer"];
}, DataManager["loadDataFile"] = function (varName, filePath) {
  console.log("[loadDataFile]", varName, "->", filePath);
  let xMLHttpRequest = new XMLHttpRequest();
  xMLHttpRequest["open"]("GET", filePath), xMLHttpRequest["overrideMimeType"]("application/json"), xMLHttpRequest["responseType"] = "arraybuffer", xMLHttpRequest.onload = function () {
    if (xMLHttpRequest["status"] < 400) {
      let textDecoder = "",
        response = xMLHttpRequest["response"];
      response = Crypto["dekit"](response, filePath, Crypto["guard"]()), textDecoder = new TextDecoder()["decode"](response), window[varName] = JSON["parse"](textDecoder), DataManager["onLoad"](window[varName]);
    }
  }, xMLHttpRequest["onerror"] = this._mapLoader || function () {
    DataManager["_errorUrl"] = DataManager._errorUrl || filePath;
  }, window[varName] = null, xMLHttpRequest["send"]();
};
const DEFAULT_INV = "ashley";
function Inventory() {}
Inventory["storage"] = {}, Inventory["current"] = DEFAULT_INV, Inventory.swap = function (current) {
  if (this.current == current) return;
  let snapshot = {},
    prevItems = {},
    current1 = this["current"];
  this["current"] = current, $gameParty.allItems()["forEach"](function (idObj) {
    if (!idObj) throw new Error("Item(s) missing from game.");
    let numItems = $gameParty["numItems"](idObj);
    snapshot[idObj.id] = numItems, $gameParty["loseItem"](idObj, numItems);
  }), this["storage"][current1] = snapshot, prevItems = this["storage"][this["current"]] || {};
  for (let itemId in prevItems) {
    if (DataManager.isItem($dataItems[itemId])) $gameParty["gainItem"]($dataItems[itemId], prevItems[itemId], false);else {
      if (DataManager["isWeapon"]($dataWeapons[itemId])) $gameParty.gainItem($dataWeapons[itemId], prevItems[itemId], false);else DataManager.isArmor($dataArmors[itemId]) && $gameParty["gainItem"]($dataArmors[itemId], prevItems[itemId], false);
    }
  }
};
const _DM_CGO = DataManager.createGameObjects;
DataManager["createGameObjects"] = function () {
  _DM_CGO.call(this), Inventory["storage"] = {}, Inventory["current"] = DEFAULT_INV;
};
const _DM_MSC = DataManager.makeSaveContents;
DataManager["makeSaveContents"] = function (includeItems = true) {
  let snapshot = {},
    call = _DM_MSC["call"](this);
  return $gameParty["allItems"]()["forEach"](function (idObj) {
    snapshot[idObj.id] = $gameParty["numItems"](idObj);
  }), Inventory["storage"][Inventory["current"]] = snapshot, call.format = FORMAT, call["invStorage"] = Inventory.storage, call["invCurrent"] = Inventory.current, call["chainData"] = Chain["areaData"], includeItems && (DataManager["metaVarsRecord"](call), DataManager["snapshotsRecord"](call)), call;
};
const _DM_ESC = DataManager.extractSaveContents;
DataManager["extractSaveContents"] = function (invStorageObj) {
  _DM_ESC.call(this, invStorageObj), Inventory["current"] = "", Inventory["storage"] = invStorageObj["invStorage"] || {}, Inventory["swap"](invStorageObj["invCurrent"] || "");
};
const MENU_ICON_MARGIN = 40;
function MenuOptions() {}
MenuOptions["iconImages"] = {}, MenuOptions["orderAndIcons"] = {
  "New Game": "new_game",
  Continue: "continue",
  Options: "options",
  Language: "language",
  "Vision Room": "vision",
  Credits: "credits",
  "Quit Game": "quit"
}, MenuOptions["labels"] = function () {
  return Object["keys"](this["orderAndIcons"]);
}, MenuOptions.init = function () {
  this["labels"]()["forEach"](label => {
    this.iconImages[label] = ImageManager["loadSystem"](this.orderAndIcons[label]);
  });
};
const _WTC_MCL = Window_TitleCommand.prototype.makeCommandList;
Window_TitleCommand["prototype"]["makeCommandList"] = function () {
  _WTC_MCL["call"](this);
  Lang["count"]() > 1 && this["addCommand"]("Language", "language");
  globalTag("vision_room") && this["addCommand"]("Vision Room", "vision");
  this["addCommand"]("Quit Game", "quit");
  let pushObj = [];
  for (let MenuOption of MenuOptions["labels"]()) {
    let findIndex = this["_list"].findIndex(nameObj => nameObj.name === MenuOption);
    findIndex > -1 && pushObj["push"](this["_list"][findIndex]);
  }
  this._list = pushObj;
};
const _ST_CCW = Scene_Title["prototype"]["createCommandWindow"];
Scene_Title["prototype"]["createCommandWindow"] = function () {
  _ST_CCW["call"](this), this._commandWindow["setHandler"]("language", this["commandLanguage"]["bind"](this)), this._commandWindow.setHandler("vision", this["commandVisionRoom"]["bind"](this)), this["_commandWindow"].setHandler("quit", this["commandQuitGame"]["bind"](this));
}, Scene_Title["prototype"]["commandLanguage"] = function () {
  this["_commandWindow"].close(), SceneManager["push"](Scene_Language);
}, Scene_Title.prototype["commandVisionRoom"] = function () {
  DataManager["setupNewGame"](), $gamePlayer["reserveTransfer"](86, 13, 9), this["_commandWindow"].close(), this["fadeOutAll"](), SceneManager["goto"](Scene_Map);
}, Scene_Title["prototype"].commandQuitGame = function () {
  this["_commandWindow"]["close"](), this["fadeOutAll"](), AudioManager["stopBgm"](), SceneManager["exit"]();
}, Window_TitleCommand["prototype"]["drawItem"] = function (index) {
  let commandName = this["commandName"](index),
    itemRectForText = this.itemRectForText(index),
    isValid = MenuOptions["iconImages"][commandName];
  if (isValid) {
    let x = itemRectForText.x,
      centeredY = itemRectForText.y + (itemRectForText["height"] - isValid["height"]) / 2;
    this["contents"]["blt"](isValid, 0, 0, isValid["width"], isValid.height, x, centeredY);
  }
  itemRectForText.x += MENU_ICON_MARGIN, this["resetTextColor"](), this["changePaintOpacity"](this.isCommandEnabled(index)), this["drawText"](Lang["translate"](commandName), itemRectForText.x, itemRectForText.y, itemRectForText["width"], "left");
};
let _sceneMenu = Scene_NCMenu || Scene_Menu,
  _windowMenu = Window_NCMenu || Window_MenuCommand;
const _WM_MCL = _windowMenu.prototype["makeCommandList"];
_windowMenu["prototype"].makeCommandList = function () {
  _WM_MCL["call"](this);
  let findIndex = this["_list"]["findIndex"](symbolObj => symbolObj.symbol === "options"),
    slice = this._list["slice"]();
  this["clearCommandList"]();
  for (let i19 = 0; i19 < slice["length"]; i19++) {
    let nameObj = slice[i19];
    if (DataManager["savesDisabled"] && nameObj["name"] == "Save") continue;
    this["addCommand"](nameObj["name"], nameObj["symbol"], nameObj.enabled, nameObj.ext), i19 === findIndex && (Lang["count"]() > 1 && this["addCommand"]("Language", "language", true, null), this.addCommand("Controls", "controls", true, null));
  }
}, _windowMenu.prototype["drawItem"] = function (index) {
  let itemRectForText = this["itemRectForText"](index),
    windowWidth = this["windowWidth"](),
    nameW = itemRectForText["width"] - windowWidth,
    commandName = this.commandName(index);
  commandName = Lang["translate"](commandName), this.resetTextColor(), this.changePaintOpacity(this["isCommandEnabled"](index)), this.drawText(commandName, itemRectForText.x, itemRectForText.y, nameW, "left");
};
const _SM_CCW = _sceneMenu["prototype"]["createCommandWindow"];
_sceneMenu["prototype"]["createCommandWindow"] = function () {
  _SM_CCW["call"](this), this["_commandWindow"]["setHandler"]("language", this["commandLanguage"]["bind"](this)), this["_commandWindow"]["setHandler"]("controls", this["commandControls"]["bind"](this));
}, _sceneMenu.prototype.commandLanguage = function () {
  this._commandWindow.close(), SceneManager["push"](Scene_Language);
}, _sceneMenu.prototype.commandControls = function () {
  SceneManager["push"](Scene_Controls);
};
function Window_Language() {
  this["initialize"].apply(this, arguments);
}
Window_Language["prototype"] = Object["create"](Window_Command["prototype"]), Window_Language["prototype"]["constructor"] = Window_Language, Window_Language.prototype["initialize"] = function () {
  Window_Command["prototype"]["initialize"]["call"](this, 0, 0), this.x = (Graphics["boxWidth"] - this["width"]) / 2, this.y = (Graphics.boxHeight - this["height"]) / 2, this["currentLanguage"] = Lang["current"]();
}, Window_Language["prototype"]["makeCommandList"] = function () {
  this.addCommand("Language", "language", true, 0);
}, Window_Language["prototype"]["windowWidth"] = function () {
  return 400;
}, Window_Language["prototype"]["numVisibleRows"] = function () {
  return 5;
}, Window_Language["prototype"]["drawItem"] = function (index) {
  this.refresh();
};
const BKG_COLOR = "#182232",
  TXT_COLOR = "#30B0CF",
  NFO_COLOR = "#EBAE69",
  SEP_COLOR = "#AAAAAA";
Window_Language["prototype"]["refresh"] = function () {
  let LANG_ICO_MARGIN = LANG_ICO_MARGIN,
    LANG_ICO_PIXELS = LANG_ICO_PIXELS,
    current = Lang["current"](),
    itemRect = this["itemRect"](0),
    lineHeight = this.lineHeight();
  this["contents"]["clear"](), this["resetTextColor"](), this.resetFontSettings(), this["drawText"](Lang["translate"](this["commandName"](0)), itemRect.x + LANG_ICO_PIXELS + LANG_ICO_MARGIN, itemRect.y, itemRect["width"] - 8 - LANG_ICO_PIXELS - LANG_ICO_MARGIN, "left"), this["drawText"](Lang.property(current, "langName"), itemRect.x + LANG_ICO_PIXELS + LANG_ICO_MARGIN, itemRect.y, itemRect["width"] - 8 - LANG_ICO_PIXELS - LANG_ICO_MARGIN, "right");
  if (Lang["isOfficial"](current)) {
    this["contents"]["fillRect"](itemRect.x, itemRect.y + lineHeight, itemRect["width"], lineHeight, BKG_COLOR), this["contents"]["fontBold"] = true, this["changeTextColor"](TXT_COLOR), this.drawText(Lang.translate("Credits"), itemRect.x + LANG_ICO_PIXELS + LANG_ICO_MARGIN, itemRect.y + lineHeight, itemRect["width"] - 8 - LANG_ICO_PIXELS, "left"), this["contents"]["fontBold"] = false;
    let contents = this["contents"],
      loadSystem = ImageManager["loadSystem"]("stamp");
    loadSystem["addLoadListener"](function () {
      contents["blt"](loadSystem, 0, 0, LANG_ICO_PIXELS, LANG_ICO_PIXELS, itemRect.x, itemRect.y + lineHeight + (lineHeight - LANG_ICO_PIXELS) / 2);
    }), this["changeTextColor"](NFO_COLOR);
  } else this["resetTextColor"](), this["contents"]["fillRect"](itemRect.x, itemRect.y + lineHeight + (lineHeight - 6) / 2, itemRect["width"], 3, SEP_COLOR);
  let property = Lang["property"](current, "langInfo");
  for (let i6 = 0; i6 < Math.min(property["length"], 3); i6++) {
    let line = property[i6];
    this["drawText"](line, itemRect.x + LANG_ICO_PIXELS + LANG_ICO_MARGIN, itemRect.y + lineHeight * (i6 + 2), itemRect["width"] - 8 - LANG_ICO_PIXELS, "left");
  }
}, Window_Language.prototype["processOk"] = function () {
  this._input(INPUT_RIGHT);
}, Window_Language["prototype"].cursorRight = function (wrap) {
  this["_input"](INPUT_RIGHT);
}, Window_Language["prototype"]["cursorLeft"] = function (wrap) {
  this._input(INPUT_LEFT);
}, Window_Language["prototype"]._input = function (step) {
  let count = Lang["count"](),
    index = Lang["index"](Lang["current"]());
  this["changeValue"]("language", Lang["key"]((index + step + count) % count));
}, Window_Language["prototype"].changeValue = function (value, symbol) {
  SoundManager["playSave"](), Lang["select"](symbol), ConfigManager["symbol"] = symbol, this["redrawItem"](this["findSymbol"](value)), SceneManager["_scene"] instanceof Scene_Language && SceneManager["_scene"]["_lngMsgWin"].refresh();
}, Window_Language["prototype"]["update"] = function () {
  Window_Command["prototype"]["update"]["call"](this), Input["isTriggered"]("cancel") && (this["currentLanguage"] != Lang.current() && (ConfigManager.save(), Lang["select"](Lang["current"]())), SceneManager["pop"](), SoundManager["playCancel"]());
};
function Window_LanguageIssue() {
  this.initialize["apply"](this, arguments);
}
Window_LanguageIssue["prototype"] = Object["create"](Window_Base["prototype"]), Window_LanguageIssue["prototype"]["constructor"] = Window_LanguageIssue, Window_LanguageIssue.prototype["refresh"] = function () {
  this["contents"].clear(), this["createContents"](), this.hide();
  return;
  let startsWithObj = "",
    current = Lang["current"](),
    file = Lang["file"](current);
  !file ? startsWithObj = "Unable to locate file info: " + current : startsWithObj = file["imsg"] || "";
  if (!startsWithObj || current == Lang["key"](2)) return;
  this.show();
  let color = "#FF5555",
    lineHeight = this.lineHeight(),
    maxW = this.width - this.textPadding() * 2;
  startsWithObj["startsWith"]("WARNING:") && (color = "#FFFF55");
  this["contents"].fontFace = "GameFont", this.contents["fontSize"] = 22, this["changeTextColor"](color);
  let processTextWrap = this["processTextWrap"](startsWithObj, maxW);
  this["height"] = Math["max"](lineHeight * (processTextWrap["length"] - 1) + this["padding"] * 2, lineHeight * 2);
  for (let i15 = 0; i15 < processTextWrap["length"]; i15++) {
    this.drawText(processTextWrap[i15], this["textPadding"](), lineHeight * i15);
  }
}, Window_LanguageIssue["prototype"]["processTextWrap"] = function (splitObj, maxW) {
  let trimObj = "",
    pushObj = [];
  for (let splitObjItem of splitObj["split"](" ")) {
    let textWidth = this.textWidth(trimObj + splitObjItem + " ");
    textWidth < maxW ? trimObj += splitObjItem + " " : (pushObj.push(trimObj.trim()), trimObj = splitObjItem);
  }
  return pushObj["push"](trimObj["trim"]()), pushObj;
};
function Scene_Language() {
  this["initialize"].apply(this, arguments);
}
Scene_Language["prototype"] = Object["create"](Scene_MenuBase["prototype"]), Scene_Language["prototype"].constructor = Scene_Language, Scene_Language["prototype"]["create"] = function () {
  Scene_MenuBase["prototype"]["create"]["call"](this), this.setBackgroundOpacity(128), this["_lngSelWin"] = new Window_Language(), this["addWindow"](this["_lngSelWin"]);
  let x = this["_lngSelWin"].x,
    msgY = this["_lngSelWin"].y + this["_lngSelWin"].height + 10,
    width = this["_lngSelWin"]["width"];
  this["_lngMsgWin"] = new Window_LanguageIssue(x, msgY, width), this.addWindow(this["_lngMsgWin"]), this["_lngMsgWin"]["refresh"]();
}, Window_Options["prototype"]["addGeneralOptions"] = function () {
  this["addCommand"]("UI Hints", "inputHint"), this["addCommand"]("Text Speed", "textSpeed"), this["addCommand"]("Auto Saves", "autoSaves"), this["addCommand"]("Fullscreen", "fullscreen"), this["addCommand"]("Run Always", "alwaysDash"), this["addCommand"]("Run Speed", "dashBonus");
}, Window_Options["prototype"]["addVolumeOptions"] = function () {
  this.addCommand("Volume BGM", "bgmVolume"), this.addCommand("Volume SFX", "seVolume");
}, Window_Options["prototype"]["statusText"] = function (index) {
  let commandSymbol = this.commandSymbol(index),
    getConfigValue = this["getConfigValue"](commandSymbol);
  const translate = Lang["translate"]("On"),
    translate1 = Lang["translate"]("Off"),
    translate2 = Lang.translate("Slow"),
    translate3 = Lang.translate("Fast"),
    translate4 = Lang["translate"]("Instant");
  if (commandSymbol === "dashBonus") return getConfigValue + "%";
  if (commandSymbol === "inputHint" || commandSymbol === "fullscreen" || commandSymbol === "alwaysDash") return getConfigValue ? translate : translate1;
  if (commandSymbol === "bgmVolume" || commandSymbol === "bgsVolume" || commandSymbol === "meVolume" || commandSymbol === "seVolume") return getConfigValue + "%";
  if (commandSymbol === "walkSpeed") return getConfigValue["toFixed"](2);
  if (commandSymbol === "textSpeed") return [translate2, translate3, translate4][getConfigValue];
  return getConfigValue;
};
const INPUT_LEFT = -1,
  INPUT_RIGHT = 1,
  WRAP_RESULT = true;
Window_Options.prototype["processOk"] = function () {
  this["_input"](INPUT_RIGHT, WRAP_RESULT);
}, Window_Options["prototype"]["cursorRight"] = function (wrap) {
  this["_input"](INPUT_RIGHT);
}, Window_Options["prototype"]["cursorLeft"] = function (wrap) {
  this["_input"](INPUT_LEFT);
}, Window_Options["prototype"]["_input"] = function (step, condition = false) {
  let index = this.index(),
    commandSymbol = this.commandSymbol(index),
    getConfigValue = this["getConfigValue"](commandSymbol);
  if (commandSymbol === "inputHint" || commandSymbol === "fullscreen" || commandSymbol === "alwaysDash") {
    this["changeValue"](commandSymbol, !getConfigValue);
    return;
  }
  let minVal = 0,
    maxVal = 100;
  if (commandSymbol["contains"]("Volume")) step *= VOL_STEP_SIZE;else {
    if (commandSymbol === "textSpeed") maxVal = 2;else {
      if (commandSymbol === "autoSaves") maxVal = DataManager["autoSaveMax"]();else commandSymbol === "dashBonus" && (minVal = 10, maxVal = 30, step *= DSH_STEP_SIZE);
    }
  }
  condition ? getConfigValue = getConfigValue.boundaryWrap(step, minVal, maxVal) : getConfigValue = (getConfigValue + step).clamp(minVal, maxVal), this["changeValue"](commandSymbol, getConfigValue);
}, Window_Options["prototype"]["changeValue"] = function (symbol, condition) {
  if (symbol === "fullscreen") this["setConfigValue"](symbol, condition), condition ? Graphics["_requestFullScreen"]() : Graphics["_cancelFullScreen"]();else {
    this["setConfigValue"](symbol, condition);
    if (symbol === "bgmVolume") ConfigManager["meVolume"] = condition;else symbol === "seVolume" && (ConfigManager.bgsVolume = condition);
  }
  SoundManager["playEquip"](), this["redrawItem"](this["findSymbol"](symbol));
}, Window_Options.prototype["drawItem"] = function (index) {
  let itemRectForText = this.itemRectForText(index),
    statusWidth = this.statusWidth(),
    nameW = itemRectForText.width - statusWidth,
    commandName = this.commandName(index);
  commandName = Lang["translate"](commandName), this["resetTextColor"](), this["changePaintOpacity"](this["isCommandEnabled"](index)), this["drawText"](commandName, itemRectForText.x, itemRectForText.y, nameW, "left"), this.drawText(this.statusText(index), nameW, itemRectForText.y, statusWidth, "right");
};
function Scene_Controls() {
  this.initialize["apply"](this, arguments);
}
Scene_Controls["prototype"] = Object["create"](Scene_Base["prototype"]), Scene_Controls["prototype"]["constructor"] = Scene_Controls, Scene_Controls["prototype"]["initialize"] = function () {
  Scene_Base["prototype"].initialize.call(this);
}, Scene_Controls["prototype"]._img = null, Scene_Controls["prototype"]["create"] = function () {
  Scene_Base["prototype"]["create"]["call"](this), !this["_img"] && (this["_img"] = ImageManager["loadPicture"]("keys")), this["_background"] = new Sprite(this["_img"]), this["addChild"](this["_background"]);
}, Scene_Controls["prototype"]["update"] = function () {
  Scene_Base.prototype["update"].call(this), (Input["isTriggered"]("ok") || Input.isTriggered("cancel") || TouchInput["isTriggered"]()) && this.popScene();
}, Scene_Controls.prototype["terminate"] = function () {
  SoundManager.playCancel(), Scene_Base["prototype"]["terminate"].call(this), this["removeChild"](this._background), this["_background"] = null;
};
const _G_SFS = Graphics["_switchFullScreen"];
Graphics["_switchFullScreen"] = function () {
  _G_SFS["call"](this), ConfigManager.fullscreen = Graphics["_isFullScreen"](), ConfigManager["save"]();
  Graphics["_isFullScreen"]() ? document["body"]["style"]["cursor"] = "none" : document.body.style["cursor"] = "default";
  if (SceneManager["_scene"] instanceof Scene_Options) {
    let optionsWindow = SceneManager["_scene"]["_optionsWindow"];
    optionsWindow["redrawItem"](optionsWindow.findSymbol("fullscreen"));
  }
}, Game_CharacterBase.prototype.realMoveSpeed = function () {
  if (this["_moveSpeed"] != 4) return this["_moveSpeed"];
  // let BASE_WALK_SPD = BASE_WALK_SPD;
  if (this.isDashing()) {
    let dashBonus = ConfigManager.dashBonus;
    BASE_WALK_SPD *= 1 + dashBonus / 100;
  }
  return BASE_WALK_SPD;
};
const _WM_USF = Window_Message["prototype"].updateShowFast;
Window_Message["prototype"]["updateShowFast"] = function () {
  let abs = Math.abs(ConfigManager["textSpeed"] - 2);
  abs === 0 ? this["_showFast"] = true : _WM_USF["call"](this);
};
const _WM_UM = Window_Message.prototype["updateMessage"];
Window_Message.prototype["updateMessage"] = function () {
  let abs = Math["abs"](ConfigManager["textSpeed"] - 2),
    call = _WM_UM.call(this);
  return abs !== 0 && this["_textState"] && this._textState.text[this["_textState"].index] !== "" && (this["_waitCount"] = abs - 1), call;
};
const _I_UGS = Input["_updateGamepadState"];
Input["_updateGamepadState"] = function (buttonsObj) {
  const S = S,
    buttonIdx = 9;
  if (buttonsObj[S(858)][buttonIdx] && buttonsObj.buttons[buttonIdx][S(663)] !== null) {
    let wasPressed = this[S(687)][buttonIdx],
      pressed = buttonsObj.buttons[buttonIdx].pressed;
    this[S(687)][buttonIdx] = pressed, !wasPressed && pressed && Graphics[S(242)]();
  }
  _I_UGS[S(670)](this, buttonsObj);
}, Sprite_Balloon["prototype"]["setup"] = function (balloonId) {
  this["_balloonId"] = balloonId, this["_duration"] = 8 * this["speed"]() + this["waitTime"](), this["_loop"] = false, this._fullDuration = this["_duration"];
}, Sprite_Balloon["prototype"]["frameIndex"] = function () {
  let t = this["_duration"] / this["_fullDuration"],
    floor = Math["floor"]((1 - t) * 8);
  return Math["max"](0, Math["min"](7, floor));
}, Sprite_Balloon["prototype"]["resetAnimation"] = function () {
  this["_duration"] = this["_fullDuration"];
}, Sprite_Balloon.prototype["update"] = function () {
  Sprite_Base["prototype"]["update"].call(this);
  if (this["_duration"] > 0) {
    this["_duration"]--;
    if (this["_loop"]) this["updateFrame"](), this["_duration"] <= 0 && (this._duration += this["_fullDuration"]);else this["_duration"] > 0 && this["updateFrame"]();
  }
}, Game_Event["prototype"]["isEnabled"] = function () {
  let listObj = this["event"]()["pages"][this["_pageIndex"]];
  if (listObj.list["length"] < 1 || listObj.list["length"] === 1 && listObj.list[0].code === 0) return false;
  let codeObj = listObj["list"][0];
  if (codeObj["code"] === 108) {
    let replace = codeObj["parameters"][0]["toLowerCase"]().replace(/\s+/g, ""),
      match = replace["match"](/enabled:(\d+)([!=><]+)(\w+|has)/);
    if (!match) return false;
    let number = Number(match[1]),
      op = match[2],
      onOff = match[3];
    if (onOff === "on" || onOff === "off") {
      let isOn = onOff === "on",
        value = $gameSwitches["value"](number);
      if (op === "==") return isOn && value || !isOn && !value;
      if (op === "!=") return isOn && !value || !isOn && value;
      return false;
    } else {
      if (onOff === "has") {
        if (op === "==") return $gameParty["hasItem"]($dataItems[number]);
        if (op === "!=") return !$gameParty["hasItem"]($dataItems[number]);
        return false;
      } else {
        let value = $gameVariables.value(number),
          number = Number(onOff);
        switch (op) {
          case "==":
            return value === number;
          case "!=":
            return value !== number;
          case ">=":
            return value >= number;
          case "<=":
            return value <= number;
          case ">":
            return value > number;
          case "<":
            return value < number;
        }
        return App.fail("Invalid comparison " + "operator for enable / " + "disable comment hint: " + codeObj.parameters[0]), false;
      }
    }
  }
  return true;
};
const HINT_Y_OFS = 78,
  BALLOON_ID = 15,
  HINT_RANGE = 0.75;
function eventHintEnable(eventId) {}
function eventHintDisable(eventId) {}
function Hint() {}
;
Hint["_change"] = 0, Hint["_active"] = false, Hint._balloon = null, Hint["delta"] = function () {
  if (Hint["_change"] > 0) {
    let deltaTime = SceneManager["_deltaTime"];
    return deltaTime / Hint["_change"];
  }
  return 0;
}, Hint["balloon"] = function () {
  return !this["_balloon"] && (this["_balloon"] = new Sprite_Balloon(), this["_balloon"]["setup"](BALLOON_ID), this._balloon["_loop"] = true, this["_balloon"]["alpha"] = 0, this._change = 0, this["_active"] = false), this["_balloon"];
}, Hint.open = function (speed = 0) {
  this["_active"] = true, this["_change"] = Math["max"](0, speed);
  let balloon = this.balloon();
  balloon["alpha"] <= 0 && (balloon["_duration"] = balloon["_fullDuration"]), balloon.alpha < 1 && speed <= 0 && (balloon["alpha"] = 1);
}, Hint["close"] = function (speed = 0) {
  this["_active"] = false, this._change = Math["max"](0, speed);
  let balloon = this["balloon"]();
  balloon["alpha"] > 0 && speed <= 0 && (balloon["alpha"] = 0);
}, Hint["process"] = function () {
  let balloon = this.balloon();
  if (!ConfigManager["inputHint"]) {
    Hint["close"]();
    return;
  }
  let eventId = $gameMap["_interpreter"]._eventId;
  if (eventId > 0) {
    let triggerObj = $gameMap["event"](eventId) || null;
    if (!triggerObj || triggerObj["_trigger"] < 4) {
      Hint["close"]();
      return;
    }
  }
  this["_active"] ? (balloon["update"](), balloon["alpha"] = Math.min(1, balloon.alpha + Hint["delta"]())) : balloon["alpha"] = Math["max"](0, balloon["alpha"] - Hint["delta"]());
  balloon.x = $gamePlayer["screenX"](), balloon.y = $gamePlayer["screenY"]() - HINT_Y_OFS;
  let x = $gamePlayer.x,
    y = $gamePlayer.y,
    realX = $gamePlayer["_realX"],
    realY = $gamePlayer["_realY"],
    curX = x,
    curY = y;
  switch ($gamePlayer.direction()) {
    case 2:
      curY++;
      break;
    case 4:
      curX--;
      break;
    case 6:
      curX++;
      break;
    case 8:
      curY--;
      break;
  }
  let lastEvent = 0;
  $gameMap["eventsXy"](x, y)["forEach"](function (isTriggerInObj) {
    if (isTriggerInObj["isTriggerIn"]([0]) && isTriggerInObj.isEnabled()) {
      lastEvent = isTriggerInObj._eventId;
      return;
    }
  }), $gameMap["eventsXy"](curX, curY)["forEach"](function (isTriggerInObj) {
    if (lastEvent > 0) return;
    if (isTriggerInObj["isTriggerIn"]([0]) && isTriggerInObj["isNormalPriority"]() && isTriggerInObj["isEnabled"]()) {
      lastEvent = isTriggerInObj["_eventId"];
      return;
    }
  });
  if (lastEvent > 0) {
    let hDist = 1 - Math.abs(x - realX),
      vDist = 1 - Math["abs"](y - realY);
    if (Math["min"](hDist, vDist) > HINT_RANGE) !this["_active"] && (this["open"](0.1), this["lastEvent"] = lastEvent);else lastEvent != this["lastEvent"] && this["close"](0.2);
  } else this["lastEvent"] = 0, this["_active"] && this["close"](0.1);
};
const _SM_S = Scene_Map["prototype"]["start"];
Scene_Map["prototype"]["start"] = function () {
  _SM_S.call(this), this["addChild"](Hint["balloon"]()), Chain["start"]();
};
const _SB_T = Scene_Base["prototype"]["terminate"];
Scene_Base.prototype["terminate"] = function () {
  _SB_T["call"](this);
  if (!SceneManager["isNextScene"](Scene_Map)) Chain["updateState"]();else (this instanceof Scene_Map || this instanceof Scene_Load || this instanceof Scene_Title) && Chain["terminate"]();
  SceneManager.isNextScene(Scene_Title) && (DataManager["savesDisabled"] = false), this instanceof Scene_Load && SceneManager["isNextScene"](Scene_Map) && (DataManager["savesDisabled"] = false);
};
const _SC_US = SceneManager.updateScene;
SceneManager["updateScene"] = function () {
  _SC_US["call"](this);
  $gameSystem && Number["isFinite"]($gameSystem["_secondsPlayed"]) && ($gameSystem["_secondsPlayed"] += this["_deltaTime"]);
  if (!this["isSceneChanging"]() && this["isCurrentSceneStarted"]() && this["_scene"] instanceof Scene_Map) {
    Hint["process"](), Chain.process();
    return;
  }
  Hint.close();
}, Game_System["prototype"]["playtime"] = function () {
  return this._secondsPlayed || 0;
}, Game_System["prototype"]["playtimeText"] = function () {
  let playtime = this["playtime"](),
    floor = Math["floor"](playtime / 60 / 60),
    padZeroObj = Math["floor"](playtime / 60) % 60,
    floor1 = Math["floor"](playtime % 60);
  return floor.padZero(2) + ":" + padZeroObj["padZero"](2) + ":" + floor1["padZero"](2);
}, Game_System["prototype"]["updatePlayTime"] = function (secondsPlayed) {
  this["_secondsPlayed"] && (this["_secondsPlayed"] += secondsPlayed);
};
let _DM_SNG_ = DataManager["setupNewGame"];
DataManager.setupNewGame = function () {
  _DM_SNG_.call(this), this.metaVarsClear(), $gameSystem["_secondsPlayed"] = 0;
};
const FONT_TYPES = [".ttf", ".otf", ".eot", ".svg", ".woff", ".woff2"];
function Font() {}
Font.size = 28, Font.face = "GameFont", Font.xfer = [], Font["list"] = ["Dotum", "SimHei", "GameFont", "Heiti TC", "sans-serif", "AppleGothic"], Font["refreshActiveWindows"] = function () {
  SceneManager._scene && SceneManager["_scene"]["_windowLayer"] && SceneManager["_scene"]["_windowLayer"]["children"]["forEach"](function (refreshObj) {
    typeof refreshObj["refresh"] === "function" && refreshObj["refresh"]();
  });
}, Font.default = function (condition = null) {
  let fontSize = 28,
    GameFont = "GameFont";
  condition ? (condition.contents["fontSize"] = fontSize, condition.contents["fontFace"] = GameFont) : (this.size = fontSize, this["face"] = GameFont);
}, Font["change"] = function (fontPath, fontSize = 28, condition = null) {
  let filename = Utils["filename"](fontPath);
  condition ? (condition["contents"]["fontFace"] = filename, condition.contents["fontSize"] = fontSize) : (Font.face = filename, Font["size"] = fontSize);
  if (this.list["includes"](filename)) return;
  let file = Lang["file"]();
  if (!file) {
    App["fail"]("Failed to find language for font: " + filename), this["default"](condition);
    return;
  }
  let fontFileObj = file["data"] || {},
    fontPath = file["path"] || "";
  if (!fontFileObj || !fontPath) {
    App.fail("Language data broken, can't find font: " + filename), this["default"](condition);
    return;
  }
  if (fontFileObj["fontFile"]) {
    try {
      if (this["xfer"]["includes"](filename)) return;
      this["xfer"]["push"](filename);
      let fontFile = fontFileObj["fontFile"],
        byteOffset = fontFile.byteOffset,
        byteLength = fontFile.byteLength,
        slice = fontFile["buffer"]["slice"](byteOffset, byteOffset + byteLength),
        blob = new Blob([slice]),
        createObjectURL = URL["createObjectURL"](blob),
        fontFace = new FontFace(filename, "url(" + createObjectURL + ")");
      fontFace["load"]()["then"](function (face) {
        document.fonts["add"](face), Font["list"]["push"](filename), Font["xfer"] = Font.xfer["filter"](f => f !== filename), Font["refreshActiveWindows"](), App["info"]("Successfully loaded font from CLD: " + filename);
      })["catch"](function (err) {
        Font["default"](condition), App["fail"]("Buffered font failed to load: " + filename, err);
      });
    } catch (err) {
      this["default"](condition), App["fail"]("Failed to process buffered font: " + filename, err);
    }
    return;
  }
  let relative = "";
  for (let toLowerCaseObj of Utils.findFiles(Utils["dirname"](fontPath), FONT_TYPES)) {
    if (toLowerCaseObj["toLowerCase"]()["includes"](filename["toLowerCase"]())) {
      let relative = Utils["relative"](App["rootPath"](), toLowerCaseObj),
        split = relative.split(/[\\/]+/);
      for (let i = 1; i < split["length"]; i++) {
        split[i] = encodeURIComponent(split[i]);
      }
      relative = split["join"]("/");
      break;
    }
  }
  if (!relative) {
    App.fail("Cannot locate font named: " + filename), this["default"](condition);
    return;
  }
  if (this["xfer"].includes(filename)) return;
  this.xfer.push(filename);
  let fontFace = new FontFace(filename, "url(" + relative + ")");
  fontFace["load"]()["then"](function (face) {
    document["fonts"]["add"](face), Font["list"]["push"](filename), Font.xfer = Font["xfer"].filter(f => f !== filename), Font["refreshActiveWindows"](), App["info"]("Successfully loaded font from file: " + relative);
  })["catch"](function (err) {
    Font["default"](condition), App.fail("Font failed to load: " + filename, err);
  });
};
const _WB_RFS = Window_Base["prototype"]["resetFontSettings"];
Window_Base["prototype"].resetFontSettings = function () {
  _WB_RFS["apply"](this, arguments), Font["face"] !== "" && this["constructor"]["name"] !== "Window_Credits" && (this["contents"]["fontFace"] = Font["face"], this["contents"]["fontSize"] = Font["size"]);
};
const LANG_DAT = ["sysLabel", "sysMenus", "holdings", "speakers", "text_lut"],
  LANG_ICO_MARGIN = 10,
  LANG_ICO_PIXELS = 26,
  VALID_EXT = [".cld", ".txt", ".csv"],
  LANG_OFFC = {
    english: {
      path: "data/9c7050ae76645487",
      alts: [],
      hash: "7b11886681707ddf"
    }
  },
  LANG_URLS = {
    "img/titles1/91b682859f543183": "img/titles1/91b682859f543183",
    "img/system/e5230bf37c4fabb0": "img/system/e5230bf37c4fabb0",
    "img/pictures/5972d18a4e7f34a8": "img/pictures/5972d18a4e7f34a8",
    "img/pictures/4bb0151c008236ca": "img/pictures/4bb0151c008236ca",
    "img/pictures/057974b069d30654": "img/pictures/057974b069d30654",
    "img/pictures/d6bb9fb199e4585f": "img/pictures/12ecbfd10eb675b4",
    "img/pictures/fe81c2234c75d03d": "img/pictures/2e165e53783c6e41",
    "img/pictures/99293c069863c881": "img/pictures/99293c069863c881",
    "img/pictures/6570aa3fc508a649": "img/pictures/6570aa3fc508a649",
    "img/pictures/47b90b8577d12e80": "img/pictures/47b90b8577d12e80",
    "img/pictures/8d0f07578e5fbaea": "img/pictures/8d0f07578e5fbaea",
    "img/pictures/ef5d18e2e8d2e126": "img/pictures/ef5d18e2e8d2e126",
    "img/pictures/efbd6b41ff1cd6ba": "img/pictures/efbd6b41ff1cd6ba",
    "img/pictures/c416013eb71664a6": "img/pictures/479165df84d30b92",
    "img/pictures/ae2230e801fa22cd": "img/pictures/a48eba44af873aff",
    "img/pictures/ff9c6f02af2029dc": "img/pictures/84fdd590cc3c85b3",
    "img/pictures/17f293d610fb1f85": "img/pictures/65c88a4e6682b6cc",
    "img/pictures/ea8e894527904f45": "img/pictures/b91461b7b53ad467",
    "img/pictures/1bcaec8105c94607": "img/pictures/b8aa41515332fe56",
    "img/pictures/0a8ca260f50fe1a8": "img/pictures/3abb168cc85141c4",
    "img/pictures/d34078e1558378b1": "img/pictures/73912dfe6983d02b",
    "img/pictures/5f7daa9cb4b2df33": "img/pictures/1f9dadbb8925302b",
    "img/pictures/08e2554c2e3479ca": "img/pictures/163c685fda78a235",
    "img/pictures/86bd6933cf8593f3": "img/pictures/502fd28fe6e562a8",
    "img/pictures/e1422a39a993b541": "img/pictures/4299baade3e059ed",
    "img/pictures/b1a4708b0d924751": "img/pictures/6f901296c784f59e",
    "img/pictures/858be6403c682b79": "img/pictures/b95c251c654a4da0",
    "img/pictures/7e15d4951fb20905": "img/pictures/3eb1e91237c141ef",
    "img/pictures/0dc4d6e9aed111e8": "img/pictures/3c5c648ad067292c",
    "img/parallaxes/0225d01841bb7841": "img/parallaxes/0225d01841bb7841",
    "img/parallaxes/f5307397750f3fe3": "img/parallaxes/f5307397750f3fe3",
    "img/parallaxes/bc71f43265e7a5aa": "img/parallaxes/bc71f43265e7a5aa",
    "img/parallaxes/8029b50924d441ef": "img/parallaxes/8029b50924d441ef",
    "img/parallaxes/c668c604c02be334": "img/parallaxes/c668c604c02be334",
    "img/parallaxes/d7e85591b98624a1": "img/parallaxes/b3012b40acba6cdb",
    "img/parallaxes/260e11252cd34d3f": "img/parallaxes/5988afb69af8153b",
    "img/parallaxes/42052f3bb8350934": "img/parallaxes/7acab711346866ac",
    "img/parallaxes/1199908f3eba2b25": "img/parallaxes/ddcc76bcc94cafff",
    "img/parallaxes/308ad376ffba93e5": "img/parallaxes/c47a9c4ef24acf60",
    "img/parallaxes/d115ec99407116e5": "img/parallaxes/7cb0d46c5adc0a03",
    "img/parallaxes/5e2b0186dcbfd67c": "img/parallaxes/f7aa74a99761a884",
    "img/parallaxes/cc5a1fb715b3bb8e": "img/parallaxes/2b700449143c77d5"
  },
  CID_FIXES = {
    yrrRQ30Q: "w7ZvbBvC",
    lcmDVkXT: "4yhN4h9P",
    cT2mmJwp: "DCRBH5zy",
    GXCRgnRB: "DxhMltBG"
  };
console.log(`lang dat : ${JSON.stringify(LANG_OFFC)}`);
function Lang() {}
Lang.files = {}, Lang.count = function () {
  return Object["keys"](this.files)["length"];
}, Lang["index"] = function (langId) {
  return Object["keys"](this["files"]).indexOf(langId);
}, Lang["default"] = function () {
  if (this.files) return this["key"](0);
  App["crash"]("Can't find default language.\nRe-installing game may fix it.");
}, Lang.current = function () {
  return ConfigManager["language"];
}, Lang["file"] = function () {
  let current = this["current"]();
  return !current && (App.fail("Language name isn't set. Using default."), current = this["default"]()), !this["files"].hasOwnProperty(current) && (App["alert"]("Translation content is missing for: " + current + "\nFalling back to English."), current = this["default"](), this.select(current)), this["files"][current];
}, Lang["key"] = function (langIdx) {
  let keys = Object.keys(this["files"]),
    maxIdx = keys.length - 1;
  if (maxIdx < 0) return App["crash"]("No language table created."), "";
  return (langIdx < 0 || langIdx > maxIdx) && (App["fail"]("Language index out of bounds."), langIdx = 0), keys[langIdx];
}, Lang.property = function (langId, propKey, fallback = null) {
  console.log(this.files);
  console.log(langId);
  langId = "english";
  let dataObj = this.files[langId] || {},
    hasOwnPropertyObj = dataObj["data"] || {};
  if (hasOwnPropertyObj["hasOwnProperty"](propKey)) return hasOwnPropertyObj[propKey];
  return App.fail("Language property missing: " + langId + ":" + propKey), fallback;
}, Lang["redirect"] = function (endsWithObj) {
  if (this.count() < 2 || this["current"]() == this.default()) return "";
  if (!endsWithObj["endsWith"](".png")) return "";
  let decodeURIComponent = decodeURIComponent(endsWithObj);
  endsWithObj = LANG_URLS[decodeURIComponent.slice(0, -Utils["ext"](decodeURIComponent)["length"])] || "";
  !endsWithObj && (endsWithObj = LANG_URLS[Utils["hashPath"](decodeURIComponent)] || "");
  let file = this.file(),
    hasOwnPropertyObj = file.data || {};
  if (file && hasOwnPropertyObj["hasOwnProperty"]("imgFiles") && hasOwnPropertyObj["imgFiles"].hasOwnProperty(endsWithObj)) return hasOwnPropertyObj["imgFiles"][endsWithObj];
  return "";
}, Lang.isOfficial = function (langId) {
  let hasOwnPropertyObj = this["files"][langId] || {};
  if (hasOwnPropertyObj["hasOwnProperty"]("offc")) return hasOwnPropertyObj.offc;
  return false;
}, Lang["hashData"] = function (data) {
  let imgSection = "",
    set = new Set(),
    set1 = new Set();
  for (const [includesObj, value] of Object["entries"](data)) {
    if (typeof value !== "object" || value === null || Array.isArray(value)) continue;
    if (includesObj === "sections") continue;
    includesObj["includes"]("img") ? (imgSection = includesObj, Object["keys"](value)["forEach"](imgKey => set.add(imgKey))) : Object["keys"](value).forEach(imgData => set1["add"](imgData));
  }
  let join = Array["from"](set)["sort"]()["join"](""),
    join1 = Array["from"](set1)["sort"]()["join"]("");
  return Utils["hashString"](join1) + Utils["hashString"](join, 4);
}, Lang.translate = function (key) {
  if (Lang.count() < 1) return key;
  let current = this["current"](),
    property = this["property"](current, "sysMenus"),
    property1 = this["property"](current, "sysLabel");
  if (property && property["hasOwnProperty"](key)) return property[key];
  if (property1 && property1["hasOwnProperty"](key)) return property1[key];
  return key;
}, Lang["label"] = function (replaceObj, condition = false) {
  let property = this.property(this["current"](), "labelLUT");
  return replaceObj = replaceObj.replace(/\(label\)\[([^\]]+)\]/g, function (match, labelKey) {
    let labelText = property && property[labelKey] ? property[labelKey] : match;
    return condition ? "<" + labelText + ">" : labelText;
  }), replaceObj;
}, Lang["lines"] = function (key) {
  let textObj = {
      text: key,
      lines: []
    },
    property = this["property"](this["current"](), "linesLUT");
  return textObj.text = textObj.text["replace"](/\(lines\)\[([^\]]+)\]/g, function (match, linesKey) {
    return property && property[linesKey] ? textObj["lines"] = property[linesKey] : textObj["lines"] = [match], "";
  }), textObj;
}, Lang["newLang"] = function (langPath) {
  return {
    data: {},
    path: langPath,
    offc: false,
    imsg: ""
  };
}, Lang["init"] = function () {
  if (Object.keys(LANG_OFFC)["length"] === 0) {
    App["crash"]("No official language data assigned in build.");
    return;
  }
  for (let Object_key of Object.keys(LANG_OFFC)) {
    console.log("lang loop");
    let join = Utils["join"](App["rootPath"](), LANG_OFFC[Object_key]["path"]);
    if (!Utils["exists"](join)) {
      App.fail("Couldn't locate language: " + join);
      continue;
    }
    this["files"][Object_key] = this["newLang"](join);
  }
  if (this["count"]() < 1) {
    App["crash"]("Language data missing.\nRe-installing game may fix it.");
    return;
  }
  let join = Utils.join(App["rootPath"](), "languages/");
  for (let toLowerCaseObj of Utils["folders"](join)) {
    let join = Utils.join(join, toLowerCaseObj, "dialogue.txt"),
      join1 = Utils["join"](join, toLowerCaseObj, "dialogue.csv");
    for (let searchPath of [join, join1]) {
      if (Utils["exists"](searchPath)) {
        App["info"]("Found custom translation at: " + searchPath), this["files"]["~" + toLowerCaseObj["toLowerCase"]()] = this["newLang"](searchPath);
        break;
      }
    }
  }
  for (let Util of Utils["findFiles"](join, [".cld"])) {
    let toLowerCaseObj = "+" + Utils.filename(Util);
    App["info"]("Found custom translation at: " + Util), this["files"][toLowerCaseObj["toLowerCase"]()] = this["newLang"](Util);
  }
}, Lang["select"] = function (language) {
  !this["files"]["hasOwnProperty"](language) && (language = this["default"]());
  let dataObj = this["files"][language];
  if (!Object["keys"](dataObj["data"])["length"]) {
    let toLowerCase = Utils["ext"](dataObj["path"])["toLowerCase"]();
    if (!toLowerCase || toLowerCase === ".cld") LANG_OFFC["hasOwnProperty"](language) && (dataObj["offc"] = LANG_OFFC[language]["hash"]), this["loadCLD"](dataObj);else {
      if (toLowerCase === ".txt") this.loadTXT(dataObj);else toLowerCase === ".csv" && this["loadCSV"](dataObj);
    }
    if (!Object["keys"](dataObj.data)["length"]) {
      if (Object.keys(LANG_OFFC)["includes"](language)) {
        ConfigManager["language"] = "", App["crash"]("Default language missing or broken: " + language + "\nTry reinstalling.");
        return;
      }
      App["alert"]("Fan translation missing or broken: " + language + "\nCheck with author."), this.select(this["default"]());
      return;
    }
    for (let oldId in CID_FIXES) {
      let newId = CID_FIXES[oldId];
      if (oldId in dataObj["data"].linesLUT) {
        let lineData = dataObj["data"]["linesLUT"][oldId];
        delete dataObj["data"]["linesLUT"][oldId], dataObj["data"]["linesLUT"][newId] = lineData, App.info("Fixed content ID: " + oldId + " -> " + newId);
      }
    }
  }
  ConfigManager["language"] = language, ConfigManager["save"](), Font["change"](dataObj["data"]["fontFace"], dataObj["data"]["fontSize"]);
  let langVers = dataObj["data"].langVers,
    Game = dataObj["data"]["sysLabel"]["Game"],
    Item = dataObj["data"]["sysLabel"]["Item"],
    File = dataObj.data["sysLabel"].File,
    Save = dataObj["data"].sysLabel["Save"],
    Load = dataObj["data"]["sysLabel"]["Load"];
  document["title"] = Game + " v" + GAME_VERSION, App["info"]("Selected language (" + language + ") -> Version " + langVers), $dataSystem && ($dataSystem.gameTitle = Game + " v" + GAME_VERSION, $dataSystem["terms"]["commands"][4] = Item, $dataSystem.terms["messages"].file = File, $dataSystem["terms"].messages["saveMessage"] = Save, $dataSystem.terms.messages["loadMessage"] = Load);
}, Lang["newData"] = function () {
  return {
    langName: "",
    langVers: "",
    langInfo: ["", "", ""],
    fontFace: "",
    fontSize: 0,
    fontFile: null,
    imgFiles: {},
    sysLabel: {},
    sysMenus: {},
    labelLUT: {},
    linesLUT: {}
  };
}, Lang["isValid"] = function (langNameObj) {
  let newData = this["newData"]();
  if (!langNameObj || !Object["keys"](langNameObj)["length"]) return App.fail("Language data missing."), false;
  for (let field in newData) {
    if (!(field in langNameObj)) return App["fail"]("Missing field: " + field), false;
    if (newData[field] && typeof langNameObj[field] !== typeof newData[field]) return App["fail"]("Mismatched type: " + field), false;
  }
  if (!langNameObj.langName["trim"]()) return App["fail"]("Missing langName."), false;
  if (langNameObj.langInfo.length < 3) return App["fail"]("Missing lines in langInfo."), false;
  if (!langNameObj["fontFace"]["trim"]()) return App["fail"]("Missing fontFace."), false;
  if (langNameObj["fontSize"] < 1) return App["fail"]("fontSize < 1."), false;
  for (let field of ["sysLabel", "sysMenus", "labelLUT", "linesLUT"]) {
    if (!Object["keys"](langNameObj[field])) return App["fail"](field + " empty."), false;
  }
  return true;
};
const LANG_SIG = Buffer["from"]("LANGDATA", "utf8");
Lang.loadCLD = function (pathObj) {
  let time = Perf["time"]();
  try {
    let length = LANG_SIG["length"],
      readFile = Utils["readFile"](pathObj["path"], null);
    if (!Buffer["isBuffer"](readFile)) {
      pathObj.imsg = "ERROR: Language file format invalid.";
      throw new Error("File is not in a valid format: " + pathObj.path);
    }
    if (!readFile.slice(0, length)["equals"](LANG_SIG)) {
      pathObj["imsg"] = "ERROR: Language file missing signature.";
      throw new Error("Invalid language file signature: " + pathObj.path);
    }
    readFile = readFile["slice"](length);
    let parse = JSON["parse"](readFile.toString("utf8"));
    if (!this["isValid"](parse)) {
      pathObj["imsg"] = "ERROR: Language data format invalid.";
      throw new Error("Unable to decompress language file: " + pathObj["path"]);
    }
    pathObj["offc"] = pathObj["offc"] === Utils["hashBytes"](readFile);
    parse["fontFile"] && (parse["fontFile"] = Buffer["from"](parse.fontFile, "base64"));
    for (let imgKey in parse["imgFiles"]) {
      let cldKey = "cld:" + Utils["hashString"](pathObj["path"] + imgKey),
        from = Buffer["from"](parse["imgFiles"][imgKey], "base64");
      App["cacheAdd"](cldKey, from), parse["imgFiles"][imgKey] = cldKey;
    }
    pathObj["data"] = parse;
  } catch (err) {
    App["fail"]("Error reading file: " + pathObj.path, err);
  }
  App["info"](pathObj.path + " loaded in: " + Perf.ms(time) + "ms");
}, Lang["mapImages"] = function (pathObj) {
  let rootPath = App["rootPath"](),
    dirname = Utils["dirname"](pathObj.path),
    replace = Utils.relative(rootPath, dirname)["replace"](/\\/g, "/"),
    values = Object["values"](LANG_URLS);
  for (let Utils_findFile of Utils.findFiles(dirname, [".png"])) {
    let matched = false,
      replace = Utils.relative(dirname, Utils_findFile)["replace"](/\\/g, "/"),
      hashPath = Utils.hashPath(replace);
    for (let sliceObj of [replace, hashPath]) {
      let ext = Utils["ext"](sliceObj);
      ext && (sliceObj = sliceObj.slice(0, -ext.length));
      if (values["includes"](sliceObj)) {
        matched = true, pathObj["data"]["imgFiles"][sliceObj] = replace + "/" + replace;
        break;
      }
    }
    !matched && App["fail"]("Unable to match image: " + Utils_findFile);
  }
}, Lang.loadTXT = function (pathObj) {
  let splitObj = "",
    time = Perf["time"](),
    path = pathObj["path"];
  try {
    splitObj = Utils["readFile"](path, "utf8");
  } catch (err) {
    App["fail"]("Error reading file: " + path, err);
    return;
  }
  let newData = this.newData(),
    depth = 0,
    trimObj = "",
    pushObj = [],
    condition = false;
  const sectionHandlers = {
    MENUS: newData["sysMenus"],
    LABELS: newData.sysLabel,
    ITEMS: newData["labelLUT"],
    SPEAKERS: newData.labelLUT
  };
  for (let trimObj of splitObj.split("\n")) {
    if (!trimObj.trim()) continue;
    trimObj = trimObj["replace"]("\r", "");
    if (trimObj["startsWith"]("[")) {
      if (trimObj["toUpperCase"]() === "[CHOICES]") {
        condition = true;
        continue;
      }
      depth = 0, trimObj = trimObj["replace"]("[", "")["replace"]("]", "");
      continue;
    }
    if (!trimObj) continue;
    depth += 1;
    let toUpperCase = trimObj["trim"]().toUpperCase();
    if (toUpperCase === "VERSION") newData["langVers"] = trimObj, trimObj = "";else {
      if (toUpperCase === "LANGUAGE") newData["langName"] = trimObj, trimObj = "";else {
        if (toUpperCase === "FONT") {
          let split = trimObj["split"](":");
          split["length"] > 1 && (trimObj = split[1].trim()), depth === 1 ? newData["fontFace"] = trimObj : (newData["fontSize"] = parseInt(trimObj), trimObj = "");
        } else {
          if (toUpperCase === "CREDITS") {
            let split = trimObj["split"](":");
            split["length"] > 1 && (trimObj = split[1]["trim"]()), newData["langInfo"][depth - 1] = trimObj, depth >= 3 && (trimObj = "");
          } else {
            if (toUpperCase in sectionHandlers) {
              if (trimObj.includes(":")) {
                let [split, split1] = trimObj["split"](":");
                split = split.trim(), split1 = split1["trim"](), split.startsWith("#") && (split = split["slice"](1)), sectionHandlers[toUpperCase][split] = split1;
              }
            } else {
              if (trimObj["startsWith"]("#")) {
                let sep = ":";
                trimObj.includes("(") && (sep = "(", condition = false);
                let split = trimObj["split"](sep);
                if (split["length"] < 2) {
                  App["fail"]("Line is missing parts." + "\nLine: " + trimObj + "\nFile: " + path);
                  return;
                }
                let slice = split[0]["trim"]()["slice"](1),
                  value = split[1]["startsWith"](" ") ? split[1]["slice"](1) : split[1];
                condition ? newData["labelLUT"][slice] = value : (pushObj = [], newData.linesLUT[slice] = pushObj);
              } else {
                if (trimObj["startsWith"](":")) {
                  if (condition) {
                    App["fail"]("Line content mismatch." + "\nLine: " + trimObj + "\nFile: " + path);
                    return;
                  }
                  trimObj = trimObj["slice"](1), trimObj["startsWith"](" ") && (trimObj = trimObj["slice"](1)), pushObj["push"](trimObj);
                }
              }
            }
          }
        }
      }
    }
  }
  App["info"](path + " parsed in: " + Perf.ms(time) + "ms"), pathObj["data"] = newData, this.mapImages(pathObj);
};
let entry = {
  "path": "data/English.cld",
  // 9c7050ae76645487
  "offc": true
};
console.log(entry);
Lang.loadCLD(entry);
Lang.files["english"] = entry;
console.log(Lang.count());
const CSV_BLOCKS = {
    MENUS: 2,
    ITEMS: 3,
    LABELS: 3,
    SECTION: 4,
    VERSION: 1,
    "CREDIT 1": 3,
    SPEAKERS: 3,
    LANGUAGE: 3,
    DESCRIPTIONS: 4
  },
  SECTION_HEADER = ["ID", "Source", "English", "Translation"];
Lang.is_header = function (cols) {
  const S = S,
    normalized = SECTION_HEADER[S(884)](col => col[S(921)]()),
    normalized = cols[S(884)](col => col[S(204)]().toUpperCase());
  return JSON[S(766)](normalized) === JSON[S(766)](normalized);
}, Lang["new_block"] = function (blockType, subtype, lengthObj) {
  if (!Object["keys"](CSV_BLOCKS)["includes"](blockType)) return false;
  if (blockType === "LANGUAGE") return lengthObj["length"] >= CSV_BLOCKS[blockType] && (lengthObj[1]["trim"]() === "Font File" || lengthObj[2].trim() === "Font Size" || lengthObj[2]["trim"]() !== "");
  if (blockType === "ITEMS") return lengthObj["length"] >= CSV_BLOCKS[blockType] && (lengthObj[1].trim() === "English" || lengthObj[2]["trim"]() === "Translation" || lengthObj[2]["trim"]() !== "");
  return true;
}, Lang.loadCSV = function (pathObj) {
  let splitObj = "",
    time = Perf["time"](),
    path = pathObj["path"];
  try {
    splitObj = Utils["readFile"](path, "utf8");
  } catch (err) {
    App.fail("Error reading file: " + path, err);
    return;
  }
  let blockType = "",
    sectionName = "",
    pushObj = [],
    inBlock = false,
    inBlock = false,
    newData = this.newData();
  for (let trimObj of splitObj["split"]("\n")) {
    trimObj = trimObj.trim();
    if (!trimObj) continue;
    let pos = 0,
      pushObj = [],
      cleaned = "",
      pos = 0,
      outerPos = 0,
      inQuote = false,
      length = trimObj.length;
    while (pos < length) {
      let ch = trimObj[pos];
      if (!inQuote && ch === '"') inQuote = true;else {
        if (ch === "“" || ch === "”") cleaned += '"';else {
          if (trimObj["substr"](pos, 2) == '""') cleaned += '"', pos += 1;else {
            if (inQuote && ch == '"') inQuote = false;else !inQuote && ch == "," ? (pushObj["push"](cleaned), cleaned = "") : cleaned += ch;
          }
        }
      }
      pos += 1, pos >= length && pushObj["push"](cleaned);
    }
    let length1 = pushObj["length"];
    if (length1 < 1 || pushObj[0]["trim"]() === "") continue;
    let toUpperCase = pushObj[0].toUpperCase();
    if (!toUpperCase["trim"]()) {
      App["fail"]("CSV first column missing." + "\nLine: " + trimObj + "\nFile: " + path);
      return;
    }
    if (this.new_block(toUpperCase, blockType, pushObj)) {
      blockType = toUpperCase, inBlock = true;
      continue;
    }
    if (inBlock && blockType === "SECTION") {
      this["is_header"](pushObj) && (inBlock = false);
      continue;
    }
    if (length1 < CSV_BLOCKS[blockType]) return App.fail("CSV missing columns." + " Total: " + CSV_BLOCKS[blockType] + " Found: " + length1 + "\nLine: " + trimObj + "\nFile: " + path), {};
    if (blockType === "VERSION") newData["langVers"] = pushObj[0], blockType = "";else {
      if (blockType === "LANGUAGE") newData["langName"] = pushObj[0], newData.fontFace = pushObj[1], newData["fontSize"] = parseInt(pushObj[2]), blockType = "";else {
        if (blockType === "CREDIT 1") newData["langInfo"] = pushObj["slice"](0, Math["min"](3, length1)), blockType = "";else {
          if (blockType === "LABELS") newData["sysLabel"][pushObj[0]] = pushObj[2].trim() ? pushObj[2] : pushObj[1];else {
            if (blockType === "MENUS") newData["sysMenus"][pushObj[0]] = pushObj[1].trim() ? pushObj[1] : pushObj[0];else {
              if (blockType === "ITEMS" || blockType === "SPEAKERS") {
                let trimObj = pushObj[0],
                  trimObj1 = pushObj[1],
                  trimObj2 = pushObj[2];
                if (!trimObj["trim"]() || !trimObj1["trim"]()) {
                  App["fail"]("Missing column data for Item." + "\nLine: " + trimObj + "\nFile: " + path);
                  return;
                }
                trimObj2 = trimObj2["trim"]() ? trimObj2 : trimObj1, newData["labelLUT"][trimObj] = trimObj2;
              } else {
                if (blockType === "SECTION" || blockType === "DESCRIPTIONS") {
                  let trimObj = pushObj[0],
                    trimObj1 = pushObj[1],
                    defaultText = pushObj[2],
                    trimObj2 = pushObj[3];
                  if (!trimObj["trim"]() || !trimObj1["trim"]()) {
                    App["fail"]("Missing column data for Section." + "\nLine: " + trimObj + "\nFile: " + path);
                    return;
                  }
                  trimObj2 = trimObj2["trim"]() ? trimObj2 : defaultText, trimObj1["toUpperCase"]()["includes"]("CHOICE") ? newData["labelLUT"][trimObj] = trimObj2 : (sectionName != trimObj && (pushObj = [], newData["linesLUT"][trimObj] = pushObj, sectionName = trimObj), pushObj["push"](trimObj2));
                } else {
                  App["fail"]("Invalid CSV parsing state.");
                  return;
                }
              }
            }
          }
        }
      }
    }
  }
  App["info"](path + " parsed in: " + Perf.ms(time) + "ms"), pathObj["data"] = newData, this["mapImages"](pathObj);
};
const _DM_OL = DataManager["onLoad"];
DataManager["onLoad"] = function (dataObj) {
  dataObj === $dataSystem && !App["isEditorBuild"]() && Lang["select"](Lang["current"]()), _DM_OL["call"](this, dataObj);
};
const MAX_LINES = 2;
Game_Interpreter.prototype.prevHeader = "", Game_Interpreter["prototype"].extraLines = [];
const _GI_C101 = Game_Interpreter["prototype"]["command101"];
Game_Interpreter.prototype.command101 = function () {
  console.log(Lang.count());
  if (Lang.count() < 1) {
    _GI_C101.call(this);
    return;
  }
  if (!$gameMessage["isBusy"]()) {
    if (this["extraLines"]["length"] > 0) {
      $gameMessage["add"](this.prevHeader);
      let min = Math["min"](this["extraLines"].length, MAX_LINES);
      for (let i14 = 0; i14 < min; i14++) {
        $gameMessage["add"](this.extraLines["shift"]());
      }
      return this["extraLines"]["length"] < 1 && this["_index"]++, this.setWaitMode("message"), false;
    }
    $gameMessage.setFaceImage(this["_params"][0], this._params[1]), $gameMessage["setBackground"](this["_params"][2]), $gameMessage["setPositionType"](this["_params"][3]);
    while (this["nextEventCode"]() === 401) {
      this._index++;
      let msgId = this["currentCommand"]()["parameters"][0],
        lines = Lang["lines"](Lang["label"](msgId));
      $gameMessage["add"](lines["text"]), this.prevHeader = lines["text"];
      if (lines["lines"].length) for (let i17 = 0; i17 < lines.lines.length; i17++) {
        i17 < MAX_LINES ? $gameMessage["add"](lines["lines"][i17]) : this["extraLines"]["push"](lines["lines"][i17]);
      }
      if (this["extraLines"]["length"] > 0) {
        while (this["_index"] >= 0 && this["currentCommand"]()["code"] !== 101) {
          this["_index"]--;
        }
        this["setWaitMode"]("message");
        return;
      }
    }
    switch (this["nextEventCode"]()) {
      case 102:
        this["_index"]++, this["setupChoices"](this["currentCommand"]()["parameters"]);
        break;
      case 103:
        this._index++, this["setupNumInput"](this.currentCommand()["parameters"]);
        break;
      case 104:
        this["_index"]++, this["setupItemChoice"](this["currentCommand"]()["parameters"]);
        break;
    }
    this._index++, this["setWaitMode"]("message");
  }
  return false;
};
const _GI_SC = Game_Interpreter["prototype"]["setupChoices"];
Game_Interpreter["prototype"]["setupChoices"] = function (lengthObj) {
  if (Lang["count"]() < 1) {
    _GI_SC["call"](this, lengthObj);
    return;
  }
  let clone = lengthObj[0]["clone"]();
  for (let i10 = 0; i10 < clone["length"]; i10++) {
    clone[i10] = Lang.label(clone[i10]);
  }
  let cancelIdx = lengthObj[1],
    defaultChoice = lengthObj.length > 2 ? lengthObj[2] : 0,
    cancelType = lengthObj["length"] > 3 ? lengthObj[3] : 2,
    posType = lengthObj["length"] > 4 ? lengthObj[4] : 0;
  cancelIdx >= clone["length"] && (cancelIdx = -2), $gameMessage["setChoices"](clone, defaultChoice, cancelIdx), $gameMessage["setChoiceBackground"](posType), $gameMessage["setChoicePositionType"](cancelType), $gameMessage["setChoiceCallback"](function (choiceIdx) {
    this._branch[this._indent] = choiceIdx;
  }["bind"](this));
};
const _WB_DIN = Window_Base["prototype"]["drawItemName"];
Window_Base["prototype"].drawItemName = function (isValid, x, y, maxW) {
  if (Lang["count"]() < 1) {
    _WB_DIN.call(this, isValid, x, y, maxW);
    return;
  }
  maxW = maxW || 312;
  console.log(`unused : ${isValid}`);
  if (isValid) {
    let iconOffset = Window_Base["_iconWidth"] + 4;
    this["resetTextColor"](), this["drawIcon"](isValid.iconIndex, x + 2, y + 2), this["drawText"](Lang["label"](isValid["name"]), x + iconOffset, y, maxW - iconOffset);
  }
};
const _WH_SI = Window_Help["prototype"]["setItem"];
Window_Help["prototype"]["setItem"] = function (descriptionObj) {
  if (Lang.count() < 1) {
    _WH_SI["call"](this, descriptionObj);
    return;
  }
  if (!descriptionObj) {
    this.setText("");
    return;
  }
  let lines = Lang["lines"](descriptionObj["description"]);
  lines["lines"].length > 0 ? this["setText"](lines["lines"]["join"]("\n")) : this["setText"](descriptionObj.description);
}, WebAudio["prototype"]._loading = async function (readObj) {
  try {
    const decodeStream = stbvorbis["decodeStream"](chunk => this["_onDecode"](chunk));
    let isFirst = true;
    while (true) {
      const {
        done: isValid,
        value: chunk
      } = await readObj["read"]();
      if (isValid) {
        decodeStream({
          eof: true
        });
        return;
      }
      let rawData = chunk;
      isFirst && (isFirst = false, rawData = Crypto["dekit"](rawData, this["_url"], Crypto["guard"]())), this._readLoopComments(rawData), decodeStream({
        data: rawData,
        eof: false
      });
    }
  } catch (err) {
    App["fail"]("Audio stream failure: ", err);
  }
}, WebAudio["prototype"]["_onXhrLoad"] = function (responseObj) {
  let dekit = Crypto.dekit(responseObj["response"], this["_url"], Crypto["guard"]());
  this._readLoopComments(new Uint8Array(dekit)), WebAudio["_context"]["decodeAudioData"](dekit, function (buffer) {
    this["_buffer"] = buffer, this._totalTime = buffer["duration"], this._loopLength > 0 && this["_sampleRate"] > 0 ? (this["_loopStart"] /= this._sampleRate, this._loopLength /= this["_sampleRate"]) : (this["_loopStart"] = 0, this["_loopLength"] = this._totalTime), this._onLoad();
  }.bind(this));
}, WebAudio.prototype["_readMetaData"] = function (buffer, start, length) {
  for (let i = start; i < start + length - 10; i++) {
    if (this._readFourCharacters(buffer, i) === "LOOP") {
      let matchObj = "";
      while (buffer[i] > 0) {
        matchObj += String.fromCharCode(buffer[i++]);
      }
      let match = matchObj.match(/LOOPSTART=([0-9]+)/);
      match && match["length"] > 1 && (this["_loopStart"] = parseInt(match[1]));
      match = matchObj["match"](/LOOPLENGTH=([0-9]+)/);
      match && match["length"] > 1 && (this["_loopLength"] = parseInt(match[1]));
      if (matchObj == "LOOPSTART" || matchObj == "LOOPLENGTH") {
        let tag = "";
        i += 16;
        while (buffer[i] > 0) {
          tag += String.fromCharCode(buffer[i++]);
        }
        matchObj == "LOOPSTART" ? this["_loopStart"] = parseInt(tag) : this["_loopLength"] = parseInt(tag);
      }
    }
  }
};
const TAU = Math.PI * 2,
  ATTACH_NONE = -1,
  ATTACH_MAIN = -2,
  CHAIN_IMAGE = "chain",
  CHAIN_RELAX = 0.3,
  PILE_RADIUS = 12,
  CHAIN_SFX = ["chain1", "chain2", "chain3"];
function Chain() {}
Chain["activeSg"] = [], Chain.cachedSg = [], Chain["areaData"] = {}, Chain["saveData"] = null, Chain["initIter"] = 0, Chain["distance"] = 0, Chain.segWidth = 0, Chain["segOffst"] = 0, Chain.attachTo = ATTACH_NONE, Chain["startPos"] = {
  x: 0,
  y: 0
}, Chain["finalPos"] = {
  x: 0,
  y: 0
}, Chain["onCanvas"] = false, Chain["segImage"] = null, Chain["loadBitmap"] = function (callback) {
  if (this["segImage"]) {
    callback(this.segImage);
    return;
  }
  let loadSystem = ImageManager["loadSystem"](CHAIN_IMAGE);
  loadSystem.addLoadListener(function () {
    Chain["segImage"] = loadSystem, callback(loadSystem);
  });
}, Chain["exists"] = function () {
  return this["activeSg"]["length"] > 0;
}, Chain["state"] = function () {
  let pushObj = [],
    pushObj1 = [],
    pushObj2 = [],
    segIdx = 1;
  for (let rotationObj of this["activeSg"]) {
    pushObj["push"](rotationObj.x), pushObj1["push"](rotationObj.y), pushObj2.push(rotationObj["rotation"]), segIdx = rotationObj["scale"].x;
  }
  return {
    seg_x: pushObj,
    seg_y: pushObj1,
    seg_r: pushObj2,
    seg_s: segIdx,
    seg_w: this.segWidth,
    seg_o: this["segOffst"],
    target: this["attachTo"],
    length: this["distance"],
    origin: Object["assign"]({}, this["startPos"]),
    endpos: Object.assign({}, this["finalPos"])
  };
}, Chain["target"] = function () {
  if (this["attachTo"] === ATTACH_MAIN) return $gamePlayer;else {
    if (this["attachTo"] > 0) return $gameMap["event"](this.attachTo);
  }
  return null;
}, Chain.isAttachedTo = function (targetId) {
  return this["exists"]() && this["target"]() == targetId;
}, Chain["process"] = function () {
  let target = this["target"]();
  if (target) {
    let tx = target["_realX"] + 0.5,
      ty = target["_realY"] + 0.5;
    for (let i3 = 0; i3 < 3; i3++) {
      this["update"](tx, ty);
    }
  }
}, Chain["solve"] = function (initIter) {
  this["initIter"] = 0;
  if (!this["exists"]()) {
    this["initIter"] = initIter;
    return;
  }
  let target = this["target"]();
  if (target) for (let i24 = 0; i24 < initIter; i24++) {
    let tx = target["_realX"] + 0.5,
      ty = target["_realY"] + 0.5;
    this["update"](tx, ty);
  }
}, Chain["update"] = function (tx, ty) {
  if (!this["exists"]()) return;
  if (!this["onCanvas"]) {
    let currentTilemap = SceneManager["currentTilemap"]();
    if (!currentTilemap) return;
    this["onCanvas"] = true;
    for (let this_activeSgItem of this.activeSg) {
      currentTilemap["addChild"](this_activeSgItem);
    }
  }
  let tileWidth = $gameMap.tileWidth();
  this["finalPos"].x = (tx - 0.08) * tileWidth, this["finalPos"].y = (ty - 0.08) * tileWidth;
  let assign = Object["assign"]({}, this.finalPos);
  for (let i16 = 0; i16 < this["activeSg"]["length"]; i16++) {
    assign = this["drag"](this["activeSg"][i16], assign);
  }
  assign = Object.assign({}, this["startPos"]);
  for (let i = this["activeSg"]["length"] - 1; i >= 0; i--) {
    assign = this["drag"](this["activeSg"][i], assign);
  }
}, Math.lerp = function (a, b, t) {
  return a + (b - a) * t;
}, Chain["drag"] = function (rotationObj, toPos) {
  let halfW = this["segWidth"] / 2,
    drag = {
      x: toPos.x - rotationObj.x,
      y: toPos.y - rotationObj.y
    },
    atan2 = Math.atan2(drag.y, drag.x),
    spring = {
      x: Math["cos"](atan2),
      y: Math["sin"](atan2)
    },
    sqrt = Math["sqrt"](drag.x * drag.x + drag.y * drag.y),
    min = Math.min(sqrt / halfW, 1);
  return min = 1 - Math["pow"](1 - min, 3), min = Math["lerp"](CHAIN_RELAX, 1, min), atan2 = Math["lerp"](rotationObj.rotation, atan2, min), rotationObj["rotation"] = atan2, rotationObj["heading"].x = Math["cos"](atan2), rotationObj["heading"].y = Math["sin"](atan2), sqrt > halfW && sqrt - halfW > 1 && (rotationObj.x = toPos.x - spring.x * halfW, rotationObj.y = toPos.y - spring.y * halfW), toPos.x = rotationObj.x - spring.x * (halfW - this["segOffst"]), toPos.y = rotationObj.y - spring.y * (halfW - this["segOffst"]), toPos;
}, Chain.start = function () {
  this["onCanvas"] = false;
  let seg_wObj = this.areaData[$gameMap["_mapId"]];
  if (!seg_wObj) return;
  let filter = Object["keys"](this["state"]())["filter"](key => !(key in seg_wObj));
  if (filter["length"] > 0) {
    App["fail"]("Missing fields: " + filter["join"](", "));
    return;
  }
  this["loadBitmap"](function (bitmap) {
    Chain.cacheSprites();
    let currentTilemap = SceneManager["currentTilemap"]();
    Chain.onCanvas = currentTilemap != null, Chain["segWidth"] = seg_wObj["seg_w"], Chain["segOffst"] = seg_wObj["seg_o"], Chain["attachTo"] = seg_wObj["target"], Chain["distance"] = seg_wObj["length"], Chain["startPos"] = seg_wObj["origin"], Chain["finalPos"] = seg_wObj["endpos"];
    let x = seg_wObj["seg_s"] || 1,
      segCount = seg_wObj["seg_x"]["length"] || 0;
    for (let i23 = 0; i23 < segCount; i23++) {
      let scaleObj = Chain["cachedSg"]["pop"]() || new Sprite(bitmap);
      scaleObj["scale"].x = x, scaleObj.scale.y = x, scaleObj.anchor.x = 0.5, scaleObj["anchor"].y = 0.5, scaleObj["rotation"] = seg_wObj["seg_r"][i23], scaleObj["heading"] = {
        x: Math["cos"](scaleObj["rotation"]),
        y: Math.sin(scaleObj["rotation"])
      }, scaleObj.x = seg_wObj["seg_x"][i23], scaleObj.y = seg_wObj["seg_y"][i23], scaleObj.z = 1, Chain["activeSg"].push(scaleObj), currentTilemap && currentTilemap["addChild"](scaleObj);
    }
  });
}, Chain["terminate"] = function () {
  this["updateState"](), this["cacheSprites"]();
}, Chain["clear"] = function () {
  this["cacheSprites"](), this.attachTo = ATTACH_NONE, delete this.areaData[$gameMap._mapId];
}, Chain["updateState"] = function () {
  if (Chain["saveData"]) this["areaData"] = Chain["saveData"], Chain["saveData"] = null;else {
    if (this["exists"]()) this["areaData"][$gameMap["_mapId"]] = this["state"]();else $gameMap && delete this["areaData"][$gameMap["_mapId"]];
  }
}, Chain["cacheSprites"] = function () {
  for (let parentObj of this.activeSg) {
    this["cachedSg"]["push"](parentObj), parentObj["parent"]["removeChild"](parentObj);
  }
  this["activeSg"] = [];
}, Chain["create"] = function (startX, startY, segCount, stiffness = 0.5, max = 0.25) {
  if (this["exists"]()) {
    App["info"]("Can't create chain when one already exists.");
    return;
  }
  this["onCanvas"] = false, this.loadBitmap(function (widthObj) {
    let currentTilemap = SceneManager.currentTilemap();
    Chain["onCanvas"] = currentTilemap != null, Chain["cacheSprites"](), stiffness = Math["max"](0.1, stiffness), max = Math.max(0, Math.min(max, 0.5));
    let slack = 1 - max,
      width = widthObj.width,
      tileWidth = $gameMap["tileWidth"](),
      totalLen = segCount * Math["sqrt"](2);
    Chain["distance"] = tileWidth * totalLen, Chain.segWidth = width * stiffness;
    let segsNeeded = Chain["distance"] / Chain["segWidth"],
      remainder = Chain.distance - (Chain["segWidth"] + (segsNeeded - 1) * slack * Chain["segWidth"]),
      ratio = remainder / (slack * Chain.segWidth),
      max = Math["max"](1, Math["floor"](segsNeeded + ratio)),
      scale = totalLen / (max - (max - 1) * max),
      x = tileWidth * scale / width;
    Chain["segWidth"] = width * x, Chain["segOffst"] = Chain.segWidth * max, Chain.startPos.x = (startX + 0.5) * tileWidth, Chain["startPos"].y = (startY + 0.5) * tileWidth;
    for (let i4 = 0; i4 < max; i4++) {
      let random = Math.random() * TAU,
        radius = Math["sqrt"](Math["random"]()) * PILE_RADIUS,
        scaleObj = Chain["cachedSg"]["pop"]() || new Sprite(widthObj);
      scaleObj["scale"].x = x, scaleObj["scale"].y = x, scaleObj["anchor"].x = 0.5, scaleObj["anchor"].y = 0.5, scaleObj.rotation = Math["random"]() * TAU, scaleObj["heading"] = {
        x: Math["cos"](scaleObj.rotation),
        y: Math["sin"](scaleObj["rotation"])
      }, scaleObj.x = Chain.startPos.x + radius * Math.cos(random), scaleObj.y = Chain["startPos"].y + radius * Math["sin"](random), scaleObj.z = 1, Chain["activeSg"]["push"](scaleObj), currentTilemap && currentTilemap.addChild(scaleObj);
    }
    Chain["solve"](Chain.initIter);
  });
}, SceneManager["currentTilemap"] = function () {
  let scene = SceneManager["_scene"];
  if (!scene || !scene["_spriteset"] || !scene["_spriteset"]["_tilemap"]) return null;
  return scene["_spriteset"]["_tilemap"];
};
const _GCBMS_ = Game_CharacterBase["prototype"]["moveStraight"];
Game_CharacterBase["prototype"]["moveStraight"] = function (dir) {
  let x = this.x,
    y = this.y;
  _GCBMS_.call(this, dir);
  if (!Chain["isAttachedTo"](this) || CHAIN_SFX["length"] < 1) return;
  if (this.x != x || this.y != y) {
    let pitch = Math["random"]() * 20 + 90,
      floor = Math.floor(Math["random"]() * CHAIN_SFX.length);
    AudioManager["playSe"]({
      name: CHAIN_SFX[floor],
      pan: 0,
      pitch: pitch,
      volume: 50
    });
  }
};